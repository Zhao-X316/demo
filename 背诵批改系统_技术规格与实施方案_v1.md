# 背诵批改系统 · 技术规格与实施方案 v1

> 面向 macOS 本地版（未签名 App）。本文件既是**技术规格说明书**，也是**给执行方（编码代理/工程师）的一步步操作手册**。
> 阅读对象：1) 审稿人（产品/你）；2) 执行编码的 AI 代理或工程师。
> 交付物：未签名 macOS App 的 `.app.zip`，最终通过飞书附件发送。

---

## 0. 关键技术决策（必读）

| 能力 | 方案 | 是否需要云 | 说明 |
|------|------|-----------|------|
| 语音转文字 ASR | **火山/豆包语音识别**（录音文件识别 API） | 是（用你的 App ID + Access Token） | 返回识别文本 + 带时间戳分句/分词，供熟练度计算使用 |
| 评价 | **纯算法本地计算**（无 LLM、无 DeepSeek） | 否 | 两套独立体系：正确率（门控）+ 熟练度（记忆质量） |
| 最终判定 | **系统按门槛给默认结论 + 人工可改** | — | 正确率门槛默认 95%；人工最终拍板 |

### 0.1 两套评价体系（核心，不可混为一谈）

| 体系 | 算什么 | 取值 | 作用 |
|------|--------|------|------|
| **正确率 Accuracy** | 背对了多少内容（文本相似度，覆盖率为主） | 0~100% | **通过门控**：≥95% 通过；<95% 未通过 → 次日生成补背任务重背 |
| **熟练度 Fluency** | 记忆质量（语速/停顿/卡顿等，背得顺不顺、牢不牢） | 0~100 → 分级 A/B/C | **复习调度**：通过后按记忆质量安排**梯度复习**（间隔重复），质量越高间隔越长 |

> 两者**不加权合并**。正确率决定"过没过、明天重不重背"；熟练度只在"已通过"时决定"以后什么时候再来复习"。

**明确不使用 DeepSeek / 任何大模型评分。** 评价逻辑全部为可单测的确定性算法，结果可复现、可解释。

**可插拔抽象保留**：
- `AsrProvider`：默认 `volcano`（火山/豆包）；换别家 ASR 只需新增 provider。
- 评价逻辑集中在 `domain` 层（正确率、熟练度、复习调度），便于调阈值/间隔阶梯。

**凭据原则**：火山 `App ID` / `Access Token` **绝不**写死进源码或打进 App 包。"设置页"录入，存本机应用数据目录（§5）。

---

## 1. 总体目标与范围

### 1.1 v1 闭环（必须交付）

1. 学生 / 内容 / 任务管理（增删改、启用状态、内容答案版本）。
2. 批量录音导入 + 文件名解析 + hash 去重（幂等导入）。
3. 异常池（解析失败 / 学生不存在 / 任务不存在 / 重复 / 音频异常）+ 人工改派 / 作废。
4. 今日看板（听音、看 ASR 文本、看正确率%、看熟练度分级、人工确认/改判）。
5. **正确率门控**：≥95% 通过；<95% → 次日补背任务（重背同一内容）。
6. **熟练度梯度复习**：通过后按记忆质量（A/B/C）安排下次复习日期；到期自动生成复习任务。
7. 火山/豆包 ASR 转写 + 纯算法评价；人工最终确认。

### 1.2 明确不做（v1 out of scope）

- 不做 Apple 正式签名 / 公证（未签名，靠右键打开放行）。
- 不做多端同步、不做服务器后端、不做账号体系。
- 不做实时流式 ASR（录音文件识别即可）。
- 不用 LLM 评价；不把正确率与熟练度合成单一综合分。

---

## 2. 技术准则（Technical Standards）

### 2.1 技术栈（固定，不得替换）

- 桌面框架：**Tauri v2**
- 前端：**React 18 + TypeScript 5 + Vite 5**
- 后端：**Rust（Tauri commands）**
- 数据库：**SQLite**（`rusqlite` bundled + `r2d2` 连接池）
- ASR：**火山/豆包 录音文件识别 API**（`reqwest`）
- 评价：**纯 Rust 算法**（`domain` 层：相似度 + 流畅度 + 间隔重复调度）

### 2.2 版本与工具链基线

- Node.js ≥ 20 LTS，包管理器统一 **npm**。
- Rust stable ≥ 1.77。
- Tauri CLI：`@tauri-apps/cli` v2。
- 目标平台：macOS（`aarch64-apple-darwin` 优先，兼容 `x86_64-apple-darwin`）。

### 2.3 代码规范

- TypeScript：`strict: true`；禁用 `any`；ESLint + Prettier。
- Rust：`cargo fmt` + `cargo clippy -- -D warnings`；错误用 `thiserror`。
- Tauri command 返回 `Result<T, AppError>`，前端统一捕获提示。
- 时间 ISO-8601 存储（UTC 存，UI 本地展示）。
- "日切"基准时区：**Asia/Shanghai (UTC+8)**，配置项可改。

### 2.4 分层与依赖方向（单向）

```
React 页面/组件
   ↓ invoke()
TS API 封装层 (src/api/*)
   ↓ Tauri IPC
Rust commands 层 (src-tauri/src/commands/*)
   ↓
Rust 领域服务层 (services: import / scoring / scheduler / task / asr)
   ↓
Rust 仓储层 (repo: 纯 SQLite) + domain(纯算法)
   ↓
SQLite
```

规则：上层只依赖下层；`commands` 不直接写 SQL，必须经 `repo`；`domain` 纯函数（正确率/熟练度/复习间隔）不依赖 Tauri/网络，便于单测。

---

## 3. 目录结构（Code Framework · 总览）

```
recitation-review/
├─ package.json / vite.config.ts / tsconfig.json / index.html
├─ src/                              # 前端 React
│  ├─ main.tsx / App.tsx
│  ├─ api/
│  │  ├─ client.ts                   # 统一 invoke + 错误处理
│  │  ├─ students.ts / contents.ts / tasks.ts
│  │  ├─ submissions.ts / verdicts.ts / import.ts / settings.ts
│  │  └─ reviews.ts                  # 复习/记忆卡片
│  ├─ types/models.ts
│  ├─ pages/
│  │  ├─ DashboardToday.tsx          # 今日看板（今日新背 + 补背 + 到期复习）
│  │  ├─ ImportBatch.tsx
│  │  ├─ AnomalyPool.tsx
│  │  ├─ MakeupPool.tsx              # 补背池（正确率未达标）
│  │  ├─ ReviewPool.tsx             # 复习池（按记忆质量到期）
│  │  ├─ Records.tsx
│  │  ├─ Manage.tsx
│  │  └─ Settings.tsx                # 火山ASR / 正确率门槛 / 复习间隔阶梯
│  ├─ components/
│  │  ├─ AudioPlayer.tsx
│  │  ├─ AccuracyBadge.tsx           # 正确率% + 通过/未通过
│  │  ├─ QualityBadge.tsx            # 熟练度 A/B/C + 下次复习日
│  │  └─ DiffView.tsx                # 答案 vs ASR 差异高亮
│  └─ styles/
├─ src-tauri/
│  ├─ Cargo.toml / tauri.conf.json / build.rs
│  ├─ binaries/                      # sidecar：ffmpeg-<triple>
│  ├─ migrations/0001_init.sql
│  └─ src/
│     ├─ main.rs / error.rs / state.rs
│     ├─ db/
│     │  ├─ mod.rs
│     │  └─ repo/{students,contents,tasks,submissions,verdicts,memory_cards,file_ledger}.rs
│     ├─ domain/                     # 纯算法，可单测
│     │  ├─ filename.rs              # 文件名解析
│     │  ├─ hashing.rs              # 文件 hash
│     │  ├─ normalize.rs            # 文本归一化（含可选拼音归一）
│     │  ├─ accuracy.rs             # 正确率：覆盖率/LCS/编辑距离
│     │  ├─ fluency.rs              # 熟练度：语速/停顿/语气词 → 分数+分级
│     │  ├─ scheduler.rs            # 梯度复习：记忆质量 → 间隔天数
│     │  └─ makeup.rs               # 补背任务生成规则
│     ├─ services/
│     │  ├─ import_service.rs
│     │  ├─ scoring_service.rs      # 编排 normalize+accuracy+fluency
│     │  ├─ task_service.rs         # 任务生成 / 日切 / 到期复习生成
│     │  ├─ scheduler_service.rs    # 通过后更新记忆卡片+排下次复习
│     │  └─ asr/{mod.rs, volcano.rs}
│     ├─ config/secrets.rs
│     └─ commands/{students,contents,tasks,submissions,import,verdicts,reviews,settings}.rs
├─ tests/
└─ docs/背诵批改系统_技术规格与实施方案_v1.md
```

---

## 4. 数据模型（SQLite Schema）

> 文件：`src-tauri/migrations/0001_init.sql`。音频**只存路径 + hash**，不默认复制进 App。

```sql
PRAGMA foreign_keys = ON;

-- 学生
CREATE TABLE students (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_no TEXT NOT NULL UNIQUE,      -- 学号：权威匹配字段
    name TEXT NOT NULL,                   -- 姓名：仅校验
    class_name TEXT,
    enabled INTEGER NOT NULL DEFAULT 1,
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);

-- 背诵内容
CREATE TABLE contents (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    content_no TEXT NOT NULL UNIQUE,
    title TEXT NOT NULL,
    answer_text TEXT NOT NULL,
    answer_version INTEGER NOT NULL DEFAULT 1,
    enabled INTEGER NOT NULL DEFAULT 1,
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);

-- 任务（kind 区分 新背/补背/复习）
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id INTEGER NOT NULL REFERENCES students(id),
    content_id INTEGER NOT NULL REFERENCES contents(id),
    kind TEXT NOT NULL DEFAULT 'normal',  -- normal|makeup|review
    due_date TEXT NOT NULL,               -- YYYY-MM-DD（Asia/Shanghai）
    status TEXT NOT NULL DEFAULT 'open',  -- open|submitted|passed|failed|reopened|closed|expired
    source_task_id INTEGER REFERENCES tasks(id),  -- 补背指向原任务
    card_id INTEGER REFERENCES memory_cards(id),  -- 复习任务指向记忆卡片
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL,
    UNIQUE(student_id, content_id, due_date, kind)
);
CREATE INDEX idx_tasks_due ON tasks(due_date, status);
CREATE INDEX idx_tasks_student ON tasks(student_id);

-- 提交
CREATE TABLE submissions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER REFERENCES tasks(id),
    student_id INTEGER REFERENCES students(id),
    content_id INTEGER REFERENCES contents(id),
    file_path TEXT NOT NULL,
    archived_path TEXT,
    file_hash TEXT NOT NULL,              -- sha256，去重键
    submit_date TEXT,
    duration_ms INTEGER,
    parsed_meta TEXT,
    asr_text TEXT,
    asr_meta TEXT,                        -- JSON：火山分句/分词+时间戳
    asr_status TEXT NOT NULL DEFAULT 'pending', -- pending|ok|failed
    anomaly_type TEXT,
    status TEXT NOT NULL DEFAULT 'pending',      -- pending|scored|confirmed|anomaly|voided
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);
CREATE UNIQUE INDEX uq_submissions_hash ON submissions(file_hash);
CREATE INDEX idx_submissions_status ON submissions(status, anomaly_type);

-- 判定（两套体系分别记录，不合并）
CREATE TABLE verdicts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    submission_id INTEGER NOT NULL REFERENCES submissions(id),
    accuracy REAL,                        -- 正确率 0~100（覆盖率为主）
    accuracy_pass INTEGER,                -- 0/1：是否 ≥ 门槛（默认95）
    fluency REAL,                         -- 熟练度 0~100
    quality TEXT,                         -- 记忆质量分级 A|B|C
    confidence REAL,                      -- 0~1（缺时间戳降低）
    scorer TEXT NOT NULL DEFAULT 'algorithm',
    answer_version INTEGER NOT NULL,      -- 评价依据的答案版本
    metrics_json TEXT,                    -- 漏背率/错字率/语速/停顿率/语气词数...
    machine_note TEXT,                    -- 可读说明
    human_result TEXT,                    -- pass|fail|reopen|NULL（默认取 accuracy_pass）
    human_note TEXT,
    decided_by TEXT,
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);
CREATE INDEX idx_verdicts_submission ON verdicts(submission_id);

-- 记忆卡片（间隔重复状态，每个 学生×内容 一张）
CREATE TABLE memory_cards (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id INTEGER NOT NULL REFERENCES students(id),
    content_id INTEGER NOT NULL REFERENCES contents(id),
    state TEXT NOT NULL DEFAULT 'learning',  -- learning|review|lapsed
    stage INTEGER NOT NULL DEFAULT 0,        -- 间隔阶梯下标
    interval_days INTEGER NOT NULL DEFAULT 0,
    ease REAL NOT NULL DEFAULT 2.5,          -- 可选 SM-2 变体用
    last_quality TEXT,                       -- A|B|C
    last_reviewed_at TEXT,
    due_date TEXT,                           -- 下次复习日 YYYY-MM-DD
    reps INTEGER NOT NULL DEFAULT 0,         -- 累计成功复习次数
    lapses INTEGER NOT NULL DEFAULT 0,       -- 累计失败（正确率未达标）次数
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL,
    UNIQUE(student_id, content_id)
);
CREATE INDEX idx_cards_due ON memory_cards(due_date, state);

-- 文件账本（幂等）
CREATE TABLE file_ledger (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    file_hash TEXT NOT NULL UNIQUE,
    first_path TEXT NOT NULL,
    submission_id INTEGER REFERENCES submissions(id),
    seen_count INTEGER NOT NULL DEFAULT 1,
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);

-- 非敏感设置（敏感凭据另存，§5）
CREATE TABLE app_settings (
    key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at TEXT NOT NULL
);
```

### 4.1 状态机

- `tasks.status`：`open → submitted → (passed | failed)`；`failed → closed` 并生成 `makeup` 任务（次日）；日切未交 `open → expired`，结转补背。
- 通过（`passed`）时更新/创建 `memory_cards`，写入下次 `due_date`；到期生成 `review` 任务。
- `submissions.status`：`pending → scored → confirmed`；异常 `→ anomaly`；作废 `→ voided`。

---

## 5. 安全准则（凭据管理）

### 5.1 存储位置

- 应用数据目录（Tauri `app_data_dir()`）：`~/Library/Application Support/<bundle-id>/`
  - `data.db`、`secrets.json`（**0600**）、`archive/`（可选音频归档）。

### 5.2 secrets.json 结构

```json
{
  "asr": {
    "provider": "volcano",
    "app_id": "",
    "access_token": "",
    "cluster": "",
    "endpoint": "",
    "language": "zh"
  }
}
```

> 正确率门槛、复习间隔阶梯、语气词表等非敏感配置存 `app_settings`（§8.5），不放 secrets。

### 5.3 红线

1. 源码 / `tauri.conf.json` / 打进包的资源**不得**含真实 App ID / Token。
2. `secrets.json` 加 `.gitignore`，仓库只提交 `secrets.example.json`。
3. 日志、错误、`parsed_meta`、`asr_meta` 不回显 Token（脱敏）。
4. 打包前自动扫描包内疑似凭据，命中报警/中止（§14.1）。
5. 发飞书前确认 zip 内无 `secrets.json`、无源码压缩包。

---

## 6. 文件导入与异常规则

### 6.1 命名规范

默认：`YYYYMMDD_学号_姓名_内容编号[_序号].扩展名`
例：`20260625_2023001_张三_C012_1.m4a`

- **学号**权威匹配；**姓名**仅校验（不一致 → `name_mismatch` 警告，不阻塞）。
- 支持扩展名：`.m4a .mp3 .wav .aac .flac`（可配置）。
- 同一学生同一天可多任务（新背/补背/复习），靠**内容编号 + 任务匹配**区分。

### 6.2 解析流程（`import_service`）

```
对每个文件：
1. sha256 → 查 file_ledger，命中 → seen_count+1，跳过（幂等），返回 "duplicate"
2. 解析文件名 → (date, student_no, name, content_no, seq?)；失败 → 'parse_error'
3. student_no 查 students；不存在/停用 → 'student_not_found'
4. content_no 查 contents；不存在/停用 → 'content_not_found'
5. 匹配 task：student_id+content_id+due_date(=解析date)，未关闭任意 kind 优先匹配；无 → 'task_not_found'
6. 姓名不一致 → warning（继续）
7. 通过 → 建 submission，读 duration_ms，status='pending'；写 file_ledger，task → submitted
8. 触发异步 ASR + 评价（正确率 + 熟练度）
```

### 6.3 异常类型枚举

`parse_error` | `student_not_found` | `content_not_found` | `task_not_found` | `duplicate` | `audio_invalid` | `name_mismatch`(warning)

### 6.4 异常池操作

改派（指定 student/content/task 重走 7~8）、作废（`voided`，留账本）、补建任务后一键重匹配。

---

## 7. ASR 适配层（火山/豆包）

### 7.1 trait

```rust
// src-tauri/src/services/asr/mod.rs
pub struct AsrWord { pub text: String, pub start_ms: u64, pub end_ms: u64 }
pub struct AsrResult { pub text: String, pub words: Vec<AsrWord>, pub duration_ms: u64 }
#[async_trait::async_trait]
pub trait AsrProvider: Send + Sync {
    async fn transcribe(&self, audio_path: &std::path::Path, lang: &str)
        -> Result<AsrResult, crate::error::AppError>;
    fn name(&self) -> &'static str;
}
```

### 7.2 火山实现 `volcano.rs`（对接要点）

- 火山引擎/豆包**录音文件识别**：上传音频（或 URL），轮询/回调取结果。
- 鉴权用设置页 `app_id` + `access_token`（+ `cluster`/资源 ID，按控制台）。
- 返回含整句文本与 `utterances`（带毫秒时间戳）→ 映射 `AsrResult.words`。
- 非 wav/采样率不符时，先用打包 `ffmpeg` sidecar 转 16k 单声道 wav。
- 具体字段/路径以火山官方文档为准（执行方核对），本层只暴露统一 `AsrResult`。

### 7.3 错误策略

- ASR 失败（鉴权/网络/限流/解码）→ `asr_status='failed'`，提交进待人工确认或 `audio_invalid`（解码失败）。**不阻塞**导入。
- 看板/异常池可对单条"重试 ASR"。
- 无 `words` 时间戳 → 熟练度降级（仅按总时长估语速），`confidence` 降低并标注。

---

## 8. 评价（纯算法，两套独立体系）

> 全部确定性、可单测、可解释。系统按门槛给默认结论，人工可改判。

### 8.1 正确率 Accuracy —— 通过门控（`domain::accuracy`）

衡量"背对了多少内容"，**以覆盖率为主**（更贴合"95% 正确"的直觉）。

1. **归一化**（`normalize`）：全角转半角、删标点/空白、可选删语气词、中文数字统一。
2. **拼音容错（默认开启）**：归一化后转拼音再比对，容忍 ASR 同音错字（Rust `pinyin` crate）。
   - 理由：背诵是"念"出来的，ASR 只能猜字，同音字（是/事/世）在语音里本就无法区分；按**读音**比对 = 评"念对没念对"，更贴合背诵场景，避免 95% 门槛被 ASR 同音误判误伤。
   - 实现：保留"汉字精确比对"与"拼音比对"两路，正确率取**两者较高值**（既奖励念对、又不惩罚同音识别误差）；声调可配（默认忽略声调，更宽松）。
3. **正确率**：
   - `coverage = LCS(answer, asr) / len(answer)`（答案被正确背出的比例）→ **正确率主指标**
   - `edit_sim = 1 - Levenshtein / max(len)`（辅助参考，写入 metrics）
   - `accuracy = round(100 * coverage)`
4. **门控**：`accuracy_pass = accuracy >= 门槛`（门槛默认 **95**，设置页可调）。
   - 通过：进入熟练度调度（§8.3）。
   - 未通过：**次日生成补背任务**（§9.1），不进入复习调度。
5. **metrics_json**：覆盖率、编辑相似度、漏背率、错字数、最长连续匹配段。

### 8.2 熟练度 Fluency —— 记忆质量（`domain::fluency`）

衡量"背得顺不顺、牢不牢"，依赖 ASR 时间戳。

指标（可配权重，`fluency = 100 - 加权惩罚`）：

- **语速** `cps = 有效字数 / 秒`，偏离理想区间扣分。
- **停顿率** `pause_ratio = 词间静音总时长 / duration`，多停顿扣分。
- **语气词/卡顿** `filler_count`：嗯/啊/那个/这个/呃（可配词表）。
- **重复/自我纠正** `repetition`：相邻重复片段。

**记忆质量分级 quality**（阈值可配）：
- **A 优**：fluency ≥ 85
- **B 良**：70 ≤ fluency < 85
- **C 中**：fluency < 70

> 无时间戳时仅用 `cps` 估算，其余惩罚置 0，`confidence` 标低。

### 8.3 梯度复习调度 —— 由记忆质量驱动（`domain::scheduler`）

**仅在正确率通过时执行。** 维护每个 `学生×内容` 的 `memory_cards`，按记忆质量决定下次复习间隔。

**间隔阶梯（天，默认，可配）**：`LADDER = [1, 2, 4, 7, 15, 30, 60, 120]`

**通过一次背诵（accuracy_pass = 1）时：**
```
q = quality(A/B/C)
if 新卡片(首次通过):
    stage = { A: 1, B: 0, C: 0 }       // 优 起步间隔更长
else:
    stage += { A: +2, B: +1, C: 0 }    // 质量越高，跳得越远
stage = clamp(stage, 0, len(LADDER)-1)
interval_days = LADDER[stage]
due_date = today + interval_days
state = 'review'; reps += 1; last_quality = q
```

**未通过（accuracy_pass = 0）时（脱档/遗忘）：**
```
不更新复习排程；按 §9.1 生成次日补背任务
卡片: state='lapsed', stage=0, lapses+=1   // 补背通过后从头排
```

> 可选 SM-2 变体：用 `ease` 因子（A:+0.1 / B:0 / C:-0.2，下限1.3），`interval = round(prev * ease)`。默认走阶梯模式，简单可解释；设置页可切换。

### 8.4 评价编排（`scoring_service` + `scheduler_service`）

```
1. 取 answer_text + answer_version
2. accuracy = domain::accuracy(answer, asr_text, cfg)  → accuracy, accuracy_pass
3. fluency  = domain::fluency(asr_words, duration, cfg) → fluency, quality
4. 写 verdicts(accuracy, accuracy_pass, fluency, quality, confidence,
              scorer='algorithm', answer_version, metrics_json, machine_note)
   human_result 默认 = accuracy_pass ? 'pass' : 'fail'（人工可改）
5. submissions.status='scored'
6. 待人工确认后（或自动）：
     若 pass → scheduler_service 更新 memory_card + 排下次复习
     若 fail → makeup_service 生成次日补背
```

`machine_note` 示例：「正确率 96%（通过，漏背 1 字），熟练度 78=B 良，下次复习 7 天后（{date}）」。

### 8.5 可配项（存 `app_settings`，设置页可改）

- `accuracy_threshold`（默认 95）
- `quality_thresholds`（A=85, B=70）
- `review_ladder`（默认 `[1,2,4,7,15,30,60,120]`）/ `scheduler_mode`（ladder|sm2）
- `fluency_weights`（语速/停顿/语气词权重）、`filler_words`、`ideal_cps`
- `use_pinyin_tolerance`（拼音容错开关，**默认 true**）、`pinyin_ignore_tone`（默认 true 忽略声调）
- `makeup_due_offset_days`（默认 1，补背在次日）

---

## 9. 补背与复习任务生成（`task_service` + `domain::makeup`）

### 9.1 补背（正确率未达标触发）

1. 人工确认/自动判 `fail` → 原 task `closed`，生成 `kind='makeup'` 任务（`source_task_id`=原 id，`due_date`=今天+`makeup_due_offset_days`，默认次日）。
2. 日切：`open` 且 `due_date < 今天`（且非复习卡片排程内）→ `expired`，结转补背。
3. **去重护栏**：同 `student_id + content_id` 同时只允许**一条未关闭补背任务**（status ∈ open/submitted/reopened），已存在则不新建。

### 9.2 到期复习生成

- 每日（启动/定时）扫描 `memory_cards`：`due_date <= 今天` 且 `state='review'` → 生成 `kind='review'` 任务（`card_id` 指向卡片），`due_date`=今天。
- **去重护栏**：同 `student_id + content_id` 同时只允许一条未关闭复习任务。
- 复习任务的提交同样走"正确率门控 + 熟练度调度"：通过 → 卡片前进、排更长间隔；未通过 → 转补背、卡片脱档重排。

```rust
// 伪代码
fn ensure_makeup(student_id, content_id, source_task_id, due) { if !exists_open(makeup) { create(makeup,...) } }
fn generate_due_reviews(today) {
    for card in cards_due(today) { if !exists_open_review(card.student_id, card.content_id) {
        create_task(kind="review", card_id=card.id, due=today); } }
}
```

---

## 10. Tauri Commands（接口边界）

```
# 学生 / 内容
students_list / students_upsert / students_set_enabled / students_import_csv
contents_list / contents_upsert(answer 变 → version+1) / contents_import_csv

# 任务
tasks_generate(due_date, scope) -> GenReport
tasks_list_today() -> TodayView        // 含 新背/补背/到期复习 三组
tasks_rollover(today) -> RolloverReport
reviews_generate_due(today) -> GenReport   // 到期复习任务生成

# 导入
import_scan_dir(dir) -> Vec<ParsedPreview>      // 预览不落库
import_commit(files) -> ImportReport            // 落库+触发 ASR/评价
import_retry_asr(submission_id) -> ()

# 提交 / 异常池
submissions_list(filter) / anomaly_list
anomaly_reassign(submission_id, student_id, content_id, task_id?)
anomaly_void(submission_id)

# 判定（含两套体系）
verdict_get(submission_id) -> Verdict           // accuracy/accuracy_pass/fluency/quality/metrics
verdict_human_decide(submission_id, result, note)   // pass|fail|reopen；驱动补背或复习排程
verdict_rescore(submission_id) -> Verdict           // 答案版本变化后重判

# 复习 / 记忆卡片
cards_list(filter) -> Vec<MemoryCard>           // 含 due_date / quality / stage
card_get(student_id, content_id) -> MemoryCard

# 设置
settings_get() -> SettingsView                  // token 脱敏
settings_set_asr(app_id, access_token, cluster, endpoint, language)
settings_set_scoring(accuracy_threshold, quality_a, quality_b, review_ladder,
                     scheduler_mode, fluency_weights, filler_words, ideal_cps,
                     use_pinyin, makeup_due_offset_days)
settings_test_asr() -> TestResult
```

---

## 11. 关键骨架代码

### 11.1 错误类型

```rust
// src-tauri/src/error.rs
#[derive(thiserror::Error, Debug)]
pub enum AppError {
    #[error("db error: {0}")] Db(String),
    #[error("not found: {0}")] NotFound(String),
    #[error("parse error: {0}")] Parse(String),
    #[error("asr error: {0}")] Asr(String),
    #[error("config error: {0}")] Config(String),
    #[error("io error: {0}")] Io(String),
}
impl serde::Serialize for AppError {
    fn serialize<S: serde::Serializer>(&self, s: S) -> Result<S::Ok, S::Error> { s.serialize_str(&self.to_string()) }
}
```

### 11.2 文件名解析（纯函数 + 单测）

```rust
// src-tauri/src/domain/filename.rs
pub struct ParsedName { pub date: String, pub student_no: String, pub name: String, pub content_no: String, pub seq: Option<u32> }
pub fn parse(file_stem: &str) -> Result<ParsedName, crate::error::AppError> {
    let parts: Vec<&str> = file_stem.split('_').collect();
    if parts.len() < 4 { return Err(crate::error::AppError::Parse(format!("字段不足: {file_stem}"))); }
    let raw = parts[0];
    if raw.len()!=8 || !raw.chars().all(|c| c.is_ascii_digit()) { return Err(crate::error::AppError::Parse(format!("日期非法: {raw}"))); }
    let date = format!("{}-{}-{}", &raw[0..4], &raw[4..6], &raw[6..8]);
    Ok(ParsedName { date, student_no: parts[1].into(), name: parts[2].into(), content_no: parts[3].into(),
                    seq: parts.get(4).and_then(|s| s.parse().ok()) })
}
#[cfg(test)] mod tests { use super::*;
    #[test] fn ok(){ let p=parse("20260625_2023001_张三_C012_1").unwrap(); assert_eq!(p.date,"2026-06-25"); assert_eq!(p.content_no,"C012"); assert_eq!(p.seq,Some(1)); }
    #[test] fn bad_date(){ assert!(parse("2026_2023001_张三_C012").is_err()); }
    #[test] fn missing(){ assert!(parse("20260625_2023001").is_err()); }
}
```

### 11.3 正确率（门控）算法

```rust
// src-tauri/src/domain/accuracy.rs
pub struct AccuracyResult { pub accuracy: f64, pub pass: bool, pub coverage: f64, pub edit_sim: f64 }

fn lcs_len(a:&[char], b:&[char])->usize{ let m=b.len(); let mut dp=vec![0usize;m+1];
  for i in 1..=a.len(){ let mut prev=0; for j in 1..=m{ let t=dp[j];
    dp[j]= if a[i-1]==b[j-1]{prev+1}else{dp[j].max(dp[j-1])}; prev=t; }} dp[m] }
fn lev(a:&[char], b:&[char])->usize{ let m=b.len(); let mut dp:Vec<usize>=(0..=m).collect();
  for i in 1..=a.len(){ let mut prev=dp[0]; dp[0]=i; for j in 1..=m{ let t=dp[j];
    let c=if a[i-1]==b[j-1]{0}else{1}; dp[j]=(dp[j]+1).min(dp[j-1]+1).min(prev+c); prev=t; }} dp[m] }

/// answer/asr 已归一化；threshold 默认 95.0
pub fn evaluate(answer:&str, asr:&str, threshold:f64) -> AccuracyResult {
    let a:Vec<char>=answer.chars().collect(); let b:Vec<char>=asr.chars().collect();
    if a.is_empty(){ return AccuracyResult{accuracy:0.0,pass:false,coverage:0.0,edit_sim:0.0}; }
    let coverage = (lcs_len(&a,&b) as f64 / a.len() as f64).min(1.0);
    let edit_sim = (1.0 - lev(&a,&b) as f64 / a.len().max(b.len()) as f64).max(0.0);
    let accuracy = (100.0*coverage).round();
    AccuracyResult{ accuracy, pass: accuracy >= threshold, coverage, edit_sim }
}
#[cfg(test)] mod tests { use super::*;
    #[test] fn perfect(){ let r=evaluate("床前明月光","床前明月光",95.0); assert_eq!(r.accuracy,100.0); assert!(r.pass); }
    #[test] fn fail_under_threshold(){ let r=evaluate("床前明月光疑是地上霜","床前明月光",95.0); assert!(!r.pass); }
}
```

### 11.4 熟练度 + 复习调度（骨架）

```rust
// src-tauri/src/domain/fluency.rs
use crate::services::asr::AsrWord;
pub enum Quality { A, B, C }
pub struct FluencyCfg { pub fillers: Vec<String>, pub ideal_cps: f64, pub a:f64, pub b:f64 }
pub struct FluencyResult { pub fluency:f64, pub quality:Quality, pub cps:f64, pub pause_ratio:f64, pub filler_count:u32, pub confidence:f64 }

pub fn evaluate(text:&str, words:&[AsrWord], duration_ms:u64, cfg:&FluencyCfg)->FluencyResult{
    let dur=(duration_ms as f64/1000.0).max(0.001);
    let n=text.chars().filter(|c|!c.is_whitespace()).count() as f64;
    let cps=n/dur;
    let mut gap=0u64; for w in words.windows(2){ if w[1].start_ms>w[0].end_ms{ gap+=w[1].start_ms-w[0].end_ms; } }
    let pause_ratio=(gap as f64/duration_ms.max(1) as f64).min(1.0);
    let filler_count:u32=cfg.fillers.iter().map(|f| text.matches(f.as_str()).count() as u32).sum();
    let speed_pen=((cfg.ideal_cps-cps).abs()/cfg.ideal_cps).min(1.0)*30.0;
    let pause_pen=pause_ratio*40.0;
    let filler_pen=(filler_count as f64*5.0).min(30.0);
    let fluency=(100.0-speed_pen-pause_pen-filler_pen).max(0.0).round();
    let quality= if fluency>=cfg.a {Quality::A} else if fluency>=cfg.b {Quality::B} else {Quality::C};
    let confidence= if words.is_empty(){0.4}else{0.9};
    FluencyResult{fluency,quality,cps,pause_ratio,filler_count,confidence}
}

// src-tauri/src/domain/scheduler.rs
use super::fluency::Quality;
pub struct SchedOut { pub stage:usize, pub interval_days:i64 }
/// 通过一次背诵后计算新阶段与间隔；first=该卡片首次通过
pub fn next(ladder:&[i64], cur_stage:usize, q:&Quality, first:bool)->SchedOut{
    let stage = if first {
        match q { Quality::A=>1, _=>0 }
    } else {
        let step = match q { Quality::A=>2, Quality::B=>1, Quality::C=>0 };
        cur_stage + step
    };
    let stage = stage.min(ladder.len()-1);
    SchedOut{ stage, interval_days: ladder[stage] }
}
#[cfg(test)] mod tests { use super::*; use super::super::fluency::Quality;
    const L:[i64;8]=[1,2,4,7,15,30,60,120];
    #[test] fn first_a_starts_longer(){ assert_eq!(next(&L,0,&Quality::A,true).interval_days,2); }
    #[test] fn a_jumps_two(){ assert_eq!(next(&L,2,&Quality::A,false).stage,4); }
    #[test] fn c_stays(){ assert_eq!(next(&L,3,&Quality::C,false).stage,3); }
}
```

### 11.5 前端 invoke 封装

```ts
// src/api/client.ts
import { invoke } from "@tauri-apps/api/core";
export async function call<T>(cmd: string, args?: Record<string, unknown>): Promise<T> {
  try { return await invoke<T>(cmd, args); }
  catch (e) { console.error(`[${cmd}]`, e); throw new Error(typeof e === "string" ? e : "操作失败"); }
}
```

---

## 12. 分步实施手册（一步步操作）

> 每步执行完跑验证，绿了再进下一步，**不要跳步**。

### Step 0 · 环境
```bash
node -v   # ≥20
cargo --version
rustup target add aarch64-apple-darwin x86_64-apple-darwin
```
### Step 1 · 初始化
```bash
npm create tauri-app@latest recitation-review -- --template react-ts --manager npm
cd recitation-review && npm install && npm run tauri dev
```
### Step 2 · Rust 依赖
`Cargo.toml` 加：`rusqlite`(bundled), `r2d2`, `r2d2_sqlite`, `serde`, `serde_json`, `thiserror`, `anyhow`, `async-trait`, `reqwest`(json,rustls-tls), `sha2`, `tokio`, `chrono`, `chrono-tz`, `pinyin`(可选)。`cd src-tauri && cargo build`
### Step 3 · 数据库与迁移
写 `0001_init.sql`（§4，含 `memory_cards`）；启动建池+迁移+`PRAGMA foreign_keys=ON`。验证：`data.db` 含全部表。
### Step 4 · domain 纯算法 + 单测（先测后用）
`filename / hashing / normalize / accuracy / fluency / scheduler / makeup` → `cargo test`
### Step 5 · repo 仓储层
各表 CRUD；`file_ledger` 按 hash upsert；`memory_cards` upsert；`tasks` 去重生成。
### Step 6 · services
`import_service`（§6.2）、`scoring_service`（§8.4）、`scheduler_service`（§8.3/§9.2）、`task_service`（日切+到期复习）、`asr/volcano`。
### Step 7 · commands + 前端 api 封装（§10）
### Step 8 · 前端页面
Settings → Manage → tasks_generate → ImportBatch → DashboardToday（新背/补背/复习三组）→ AnomalyPool → MakeupPool → ReviewPool → Records。
### Step 9 · sidecar ffmpeg
`binaries/` 放带 triple 的 `ffmpeg`，`tauri.conf.json` 配 `externalBin`；转 16k 单声道 wav。
### Step 10 · 集成测试
样例：3 学生、2 内容、若干命名录音（含错误命名/重复/无任务；含 ≥95% 与 <95% 各一）。跑通：生成任务 → 导入 → 火山 ASR → 正确率门控 + 熟练度分级 → 通过排复习 / 未通过排补背 → 看板人工确认。
### Step 11 · 打包：`npm run tauri build`
### Step 12 · 安全扫描 + 打 zip（§14.1）　### Step 13 · 飞书发送（§14.2）

---

## 13. 验收标准（Acceptance Standards）

### 13.1 功能验收清单

- [ ] App 在目标 Mac 双击启动；未签名提示可右键打开/系统设置放行。
- [ ] 导入学生/内容（CSV），生成今日任务。
- [ ] 批量导入命名规范音频，正确匹配 学生/日期/内容/任务。
- [ ] 重复导入同一文件（同 hash）不重复建提交、不重复评价（幂等）。
- [ ] 命名错误/学生不匹配/任务不存在 进异常池，可改派或作废。
- [ ] 设置页填火山凭据后 `settings_test_asr` 成功。
- [ ] 测试音频完成火山转写，并算出**正确率% + 是否通过 + 熟练度分 + 记忆质量 A/B/C**（含可读说明）。
- [ ] **正确率 ≥ 门槛（默认95）→ 通过**；**< 门槛 → 未通过 → 次日生成补背任务**。
- [ ] **通过后按记忆质量写入下次复习日**：A 优间隔最长、B 良居中、C 中最短；到期能自动生成复习任务。
- [ ] 复习任务再次背诵：通过则间隔变长，未通过则转补背且卡片脱档重排。
- [ ] 同一学生同一内容：补背、复习 各自同时只有一条未关闭任务（无增殖）。
- [ ] ASR 失败不阻塞主流程，可重试；无时间戳时熟练度降级且标注。
- [ ] 改答案后 `verdict_rescore` 按新 `answer_version` 重判。
- [ ] 产物为 `.app.zip`，不含源码压缩包/`secrets.json`/明文 Token。

### 13.2 自动化测试计划

- **单元测试**：文件名解析、hash 去重、文本归一化、正确率门控（含 95% 边界）、熟练度分级、复习间隔（首次/A跳2/B跳1/C不动/封顶）、补背生成。
- **数据库测试**：任务生成幂等、重复提交处理、异常改派、答案版本重判、记忆卡片 upsert 与到期查询。
- **集成测试**：≥95% 与 <95% 两条样例分别走通"复习排程"与"补背"分支。
- **ASR 容错**：有效凭据成功；无/错凭据、网络失败 → 可恢复错误。
- **打包验收**：`npm run tauri build` 出包，zip 解压可重启。

### 13.3 判定：13.1 全勾 + 13.2 全绿 = v1 通过。

---

## 14. 打包安全扫描与飞书发送

### 14.1 zip + 扫描（示意）
```bash
APP="src-tauri/target/release/bundle/macos/RecitationReview.app"
find "$APP" -name "secrets.json" -print -quit | grep -q . && { echo "❌ 含 secrets.json"; exit 1; }
grep -rIl --binary-files=without-match -E "access_token|[A-Za-z0-9]{32,}" "$APP" 2>/dev/null | grep -q . && echo "⚠️ 检测到疑似凭据，请人工复核"
ditto -c -k --sequesterRsrc --keepParent "$APP" RecitationReview.app.zip
echo "✅ 产物：RecitationReview.app.zip"
```

### 14.2 飞书发送
- 目标"发给我"；用飞书附件脚本/Hermes 发送**真实文件附件**（非链接）；确认返回 success，核对大小与文件名。

---

## 15. 已确认决策 / 假设

1. **ASR = 火山/豆包**（你提供 App ID + Access Token），不用本机 whisper、不用 DeepSeek。
2. **正确率与熟练度是两套独立体系，不合并为综合分。**
   - 正确率：覆盖率为主的文本相似度；**门槛默认 95%**，≥95% 通过，<95% 次日补背重背。**默认开启拼音容错**（忽略声调，汉字/拼音两路取较高值），以贴合语音背诵、避免同音字误判。
   - 熟练度：语速/停顿/语气词 → 0~100 → 分级 A/B/C，**仅用于通过后的梯度复习排程**。
3. **梯度复习**：记忆质量越高，下次复习间隔越长（默认阶梯 `[1,2,4,7,15,30,60,120]` 天，A 跳 2 档 / B 跳 1 档 / C 原地；可切 SM-2 ease 变体）。门槛、阶梯、分级线均设置页可调。
4. 技术栈固定 Tauri，不改 Electron / 纯 Web；不做 Apple 签名/公证。
5. 火山凭据仅本机配置与测试，不入库源码、不打进包。
6. 日切时区 Asia/Shanghai (UTC+8)。
7. 熟练度依赖火山时间戳；无时间戳降级为按总时长估语速并降 confidence。

> 审稿通过后，执行方按 §12 逐步进入编码。
