# 背诵批改系统 · 技术规格与实施方案 v1

> 面向 macOS 本地版（未签名 App）。本文件既是**技术规格说明书**，也是**给执行方（编码代理/工程师）的一步步操作手册**。
> 阅读对象：1) 审稿人（产品/你）；2) 执行编码的 AI 代理或工程师。
> 交付物：未签名 macOS App 的 `.app.zip`，最终通过飞书附件发送。

---

## 0. 关键技术决策（必读）

| 能力 | 方案 | 是否需要云 | 说明 |
|------|------|-----------|------|
| 语音转文字 ASR | **火山/豆包语音识别**（录音文件识别 API） | 是（用你的 App ID + Access Token） | 返回识别文本 + 带时间戳的分句/分词，供熟练度评分使用 |
| 评分（正确率 + 熟练度） | **纯算法本地计算**（无 LLM、无 DeepSeek） | 否 | 正确率=文本相似度；熟练度=语速/停顿/语气词等声学+文本指标 |
| 最终判定 | **人工确认** | — | 系统只给参考分，老师点 通过/未通过/打回 |

**明确不使用 DeepSeek / 任何大模型评分。** 评分逻辑全部为可单测的确定性算法，结果可复现、可解释。

**可插拔抽象保留**：
- `AsrProvider`：今天默认 `volcano`（火山/豆包）；未来要换别家 ASR，新增一个 provider 即可。
- `Scorer`：今天默认 `algorithm`（双维度算法）；逻辑集中在 `domain` 层，便于调权重、加指标。

**凭据原则**：火山 `App ID` / `Access Token` **绝不**写死进源码或打进 App 包。App"设置页"录入，保存到本机应用数据目录（见 §5）。

---

## 1. 总体目标与范围

### 1.1 v1 闭环（必须交付）

1. 学生 / 内容 / 任务管理（增删改、启用状态、内容答案版本）。
2. 批量录音导入 + 文件名解析 + hash 去重（幂等导入）。
3. 异常池（解析失败 / 学生不存在 / 任务不存在 / 重复 / 音频异常）+ 人工改派 / 作废。
4. 今日看板（听音、看 ASR 文本、看正确率分+熟练度分、人工判定 通过/未通过/打回）。
5. 补背任务（未通过或日切未交 → 生成补背；同一学生同一内容仅一条未关闭补背）。
6. 火山/豆包 ASR 转写 + 纯算法参考评分；人工最终确认。

### 1.2 明确不做（v1 out of scope）

- 不做 Apple 正式签名 / 公证（未签名，靠右键打开放行）。
- 不做多端同步、不做服务器后端、不做账号体系。
- 不做实时流式 ASR（批处理/录音文件识别即可）。
- 不用 LLM 评分；不自动判定"通过/未通过"——系统只给**参考分**，人工拍板。

---

## 2. 技术准则（Technical Standards）

### 2.1 技术栈（固定，不得替换）

- 桌面框架：**Tauri v2**
- 前端：**React 18 + TypeScript 5 + Vite 5**
- 后端：**Rust（Tauri commands）**
- 数据库：**SQLite**（`rusqlite` bundled + `r2d2` 连接池，零网络依赖、编译简单）
- ASR：**火山/豆包 录音文件识别 API**（`reqwest` 调用）
- 评分：**纯 Rust 算法**（`domain` 层，文本相似度 + 声学/文本流畅度指标）

### 2.2 版本与工具链基线

- Node.js ≥ 20 LTS，包管理器统一 **npm**（不混用 pnpm/yarn）。
- Rust stable ≥ 1.77。
- Tauri CLI：`@tauri-apps/cli` v2。
- 目标平台：macOS（`aarch64-apple-darwin` 优先，同时支持 `x86_64-apple-darwin`）。

### 2.3 代码规范

- TypeScript：`strict: true`；禁用 `any`；ESLint + Prettier。
- Rust：`cargo fmt` + `cargo clippy -- -D warnings`；错误用 `thiserror`，`anyhow` 仅最外层。
- 所有 Tauri command 返回 `Result<T, AppError>`，前端统一捕获提示。
- 时间统一 ISO-8601 存储（UTC 存，UI 本地时区展示）。
- "日切"基准时区：**Asia/Shanghai (UTC+8)**，配置项可改。

### 2.4 分层与依赖方向（单向，禁止反向依赖）

```
React 页面/组件
   ↓ invoke()
TS API 封装层 (src/api/*)
   ↓ Tauri IPC
Rust commands 层 (src-tauri/src/commands/*)
   ↓
Rust 领域服务层 (services: import / scoring / makeup / asr)
   ↓
Rust 仓储层 (repo: 纯 SQLite 读写) + domain(纯算法)
   ↓
SQLite
```

规则：上层只能依赖下层；`commands` 不直接写 SQL，必须经 `repo`；`domain` 是纯函数（评分、解析、相似度）不依赖 Tauri/网络，便于单测。

---

## 3. 目录结构（Code Framework · 总览）

```
recitation-review/
├─ package.json
├─ vite.config.ts
├─ tsconfig.json
├─ index.html
├─ src/                              # 前端 React
│  ├─ main.tsx
│  ├─ App.tsx
│  ├─ api/
│  │  ├─ client.ts                   # 统一 invoke + 错误处理
│  │  ├─ students.ts
│  │  ├─ contents.ts
│  │  ├─ tasks.ts
│  │  ├─ submissions.ts
│  │  ├─ verdicts.ts
│  │  ├─ import.ts
│  │  └─ settings.ts
│  ├─ types/
│  │  └─ models.ts                   # 与 Rust 对齐的 TS 类型
│  ├─ pages/
│  │  ├─ DashboardToday.tsx          # 今日看板
│  │  ├─ ImportBatch.tsx             # 批量导入
│  │  ├─ AnomalyPool.tsx             # 异常池
│  │  ├─ MakeupPool.tsx              # 补背任务池
│  │  ├─ Records.tsx                 # 成绩记录
│  │  ├─ Manage.tsx                  # 学生/内容管理
│  │  └─ Settings.tsx                # 火山 ASR / 评分权重 设置
│  ├─ components/
│  │  ├─ AudioPlayer.tsx
│  │  ├─ ScoreBadge.tsx              # 正确率 + 熟练度 双分展示
│  │  └─ DiffView.tsx                # 答案 vs ASR 文本差异高亮
│  └─ styles/
├─ src-tauri/                        # 后端 Rust
│  ├─ Cargo.toml
│  ├─ tauri.conf.json
│  ├─ build.rs
│  ├─ binaries/                      # sidecar：ffmpeg-<triple>（音频转码用）
│  ├─ migrations/
│  │  └─ 0001_init.sql
│  └─ src/
│     ├─ main.rs
│     ├─ error.rs
│     ├─ state.rs
│     ├─ db/
│     │  ├─ mod.rs
│     │  └─ repo/
│     │     ├─ students.rs
│     │     ├─ contents.rs
│     │     ├─ tasks.rs
│     │     ├─ submissions.rs
│     │     ├─ verdicts.rs
│     │     └─ file_ledger.rs
│     ├─ domain/                     # 纯算法，可单测，不依赖 Tauri/网络
│     │  ├─ filename.rs              # 文件名解析
│     │  ├─ hashing.rs              # 文件 hash
│     │  ├─ normalize.rs            # 文本归一化（含可选拼音归一）
│     │  ├─ accuracy.rs             # 正确率：LCS/编辑距离/漏错统计
│     │  ├─ fluency.rs              # 熟练度：语速/停顿/语气词
│     │  ├─ score.rs                # 综合参考分（加权）
│     │  └─ makeup.rs               # 补背任务生成规则
│     ├─ services/
│     │  ├─ import_service.rs
│     │  ├─ scoring_service.rs      # 编排 normalize+accuracy+fluency+score
│     │  ├─ task_service.rs
│     │  └─ asr/
│     │     ├─ mod.rs               # trait AsrProvider + AsrResult(含时间戳)
│     │     └─ volcano.rs           # 火山/豆包 录音文件识别
│     ├─ config/
│     │  └─ secrets.rs              # 凭据读写（应用数据目录）
│     └─ commands/
│        ├─ mod.rs
│        ├─ students.rs
│        ├─ contents.rs
│        ├─ tasks.rs
│        ├─ submissions.rs
│        ├─ import.rs
│        ├─ verdicts.rs
│        └─ settings.rs
├─ tests/
└─ docs/
   └─ 背诵批改系统_技术规格与实施方案_v1.md
```

---

## 4. 数据模型（SQLite Schema）

> 文件：`src-tauri/migrations/0001_init.sql`。音频**只存路径 + hash**，不默认复制进 App；导入后可选归档。

```sql
PRAGMA foreign_keys = ON;

-- 学生
CREATE TABLE students (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    student_no  TEXT NOT NULL UNIQUE,        -- 学号：权威匹配字段
    name        TEXT NOT NULL,               -- 姓名：仅校验
    class_name  TEXT,
    enabled     INTEGER NOT NULL DEFAULT 1,
    created_at  TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);

-- 背诵内容
CREATE TABLE contents (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    content_no      TEXT NOT NULL UNIQUE,
    title           TEXT NOT NULL,
    answer_text     TEXT NOT NULL,
    answer_version  INTEGER NOT NULL DEFAULT 1,   -- 改答案 +1
    enabled         INTEGER NOT NULL DEFAULT 1,
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);

-- 任务
CREATE TABLE tasks (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id      INTEGER NOT NULL REFERENCES students(id),
    content_id      INTEGER NOT NULL REFERENCES contents(id),
    due_date        TEXT NOT NULL,               -- YYYY-MM-DD（Asia/Shanghai）
    status          TEXT NOT NULL DEFAULT 'open',-- open|submitted|passed|failed|reopened|closed|expired
    is_makeup       INTEGER NOT NULL DEFAULT 0,
    source_task_id  INTEGER REFERENCES tasks(id),
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL,
    UNIQUE(student_id, content_id, due_date, is_makeup)
);
CREATE INDEX idx_tasks_due ON tasks(due_date, status);
CREATE INDEX idx_tasks_student ON tasks(student_id);

-- 提交
CREATE TABLE submissions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id         INTEGER REFERENCES tasks(id),
    student_id      INTEGER REFERENCES students(id),
    content_id      INTEGER REFERENCES contents(id),
    file_path       TEXT NOT NULL,
    archived_path   TEXT,
    file_hash       TEXT NOT NULL,                -- sha256，去重键
    submit_date     TEXT,
    duration_ms     INTEGER,                      -- 音频时长（评分用）
    parsed_meta     TEXT,                         -- JSON：解析结果
    asr_text        TEXT,
    asr_meta        TEXT,                         -- JSON：火山返回的分句/分词+时间戳
    asr_status      TEXT NOT NULL DEFAULT 'pending', -- pending|ok|failed
    anomaly_type    TEXT,                         -- NULL=正常；见 §6
    status          TEXT NOT NULL DEFAULT 'pending', -- pending|scored|confirmed|anomaly|voided
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
CREATE UNIQUE INDEX uq_submissions_hash ON submissions(file_hash);
CREATE INDEX idx_submissions_status ON submissions(status, anomaly_type);

-- 判定（双维度参考分 + 人工结论）
CREATE TABLE verdicts (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    submission_id   INTEGER NOT NULL REFERENCES submissions(id),
    accuracy_score  REAL,                        -- 正确率 0~100
    fluency_score   REAL,                        -- 熟练度 0~100
    ref_score       REAL,                        -- 综合参考分（加权）
    confidence      REAL,                        -- 0~1（缺时间戳等会降低）
    scorer          TEXT NOT NULL DEFAULT 'algorithm',
    answer_version  INTEGER NOT NULL,            -- 评分依据的答案版本（重判用）
    metrics_json    TEXT,                        -- JSON：漏背率/错字率/语速/停顿率/语气词数...
    machine_note    TEXT,                        -- 算法生成的可读说明
    human_result    TEXT,                        -- pass|fail|reopen|NULL
    human_note      TEXT,
    decided_by      TEXT,
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
CREATE INDEX idx_verdicts_submission ON verdicts(submission_id);

-- 文件账本（幂等）
CREATE TABLE file_ledger (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    file_hash       TEXT NOT NULL UNIQUE,
    first_path      TEXT NOT NULL,
    submission_id   INTEGER REFERENCES submissions(id),
    seen_count      INTEGER NOT NULL DEFAULT 1,
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);

-- 非敏感设置（敏感凭据另存，见 §5）
CREATE TABLE app_settings (
    key         TEXT PRIMARY KEY,
    value       TEXT NOT NULL,
    updated_at  TEXT NOT NULL
);
```

### 4.1 状态机

- `tasks.status`：`open → submitted → (passed | failed)`；`failed → closed` 并生成补背；日切未交 `open → expired` 并结转补背。
- `submissions.status`：`pending → scored → confirmed`；异常 `→ anomaly`；作废 `→ voided`。
- `submissions.asr_status`：`pending → ok | failed`。

---

## 5. 安全准则（凭据管理）

### 5.1 存储位置

- 应用数据目录（Tauri `app_data_dir()`）：`~/Library/Application Support/<bundle-id>/`
  - `data.db`：SQLite。
  - `secrets.json`：火山凭据，**权限 0600**。
  - `archive/`：可选音频归档。

### 5.2 secrets.json 结构

```json
{
  "asr": {
    "provider": "volcano",
    "app_id": "",
    "access_token": "",
    "cluster": "",
    "endpoint": "",
    "language": "zh"
  }
}
```

> 评分权重等非敏感配置存 `app_settings` 表（见 §8.4），不放 secrets。

### 5.3 红线

1. 源码、`tauri.conf.json`、打进包的资源中**不得**出现真实 App ID / Token。
2. `secrets.json` 加入 `.gitignore`；仓库只提交 `secrets.example.json`。
3. 日志、错误提示、`parsed_meta`、`asr_meta` 中**不得**回显 Token（打印脱敏）。
4. 打包脚本产出 `.app.zip` 前自动扫描包内是否含疑似 Token 字符串，命中即中止（§14.1）。
5. 发飞书前确认 zip 内无 `secrets.json`、无源码压缩包。

---

## 6. 文件导入与异常规则

### 6.1 命名规范

默认：`YYYYMMDD_学号_姓名_内容编号[_序号].扩展名`
例：`20260625_2023001_张三_C012_1.m4a`

- **学号**为权威匹配字段；**姓名**只做校验（不一致 → `name_mismatch` 警告，不阻塞）。
- 支持扩展名：`.m4a .mp3 .wav .aac .flac`（可配置）。
- 同一学生同一天可多任务，靠**内容编号**区分。

### 6.2 解析流程（`import_service`）

```
对每个文件：
1. 计算 sha256 → 查 file_ledger
   命中 → seen_count+1，跳过建提交（幂等），返回 "duplicate"
2. 解析文件名 → (date, student_no, name, content_no, seq?)
   失败 → anomaly_type='parse_error', status='anomaly'
3. 按 student_no 查 students；不存在/停用 → 'student_not_found'
4. 按 content_no 查 contents；不存在/停用 → 'content_not_found'
5. 匹配 task：student_id+content_id+due_date(=解析date)；不存在 → 'task_not_found'
6. 姓名校验：不一致 → 记 warning（仍继续）
7. 通过 → 建 submission（关联 task/student/content），读取 duration_ms，status='pending'
   写 file_ledger，task.status → submitted
8. 触发异步 ASR + 评分流水线
```

### 6.3 异常类型枚举

`parse_error` | `student_not_found` | `content_not_found` | `task_not_found` | `duplicate` | `audio_invalid` | `name_mismatch`(warning)

### 6.4 异常池操作

- **改派**：手动指定 student/content/task，重走第 7~8 步。
- **作废**：`status='voided'`（保留账本）。
- **补建任务后重试**：对异常提交一键重匹配。

---

## 7. ASR 适配层（火山/豆包）

### 7.1 trait

```rust
// src-tauri/src/services/asr/mod.rs
pub struct AsrWord { pub text: String, pub start_ms: u64, pub end_ms: u64 }
pub struct AsrResult {
    pub text: String,            // 整段文本
    pub words: Vec<AsrWord>,     // 分词/分句 + 时间戳（熟练度用；可能为空）
    pub duration_ms: u64,
}
#[async_trait::async_trait]
pub trait AsrProvider: Send + Sync {
    async fn transcribe(&self, audio_path: &std::path::Path, lang: &str)
        -> Result<AsrResult, crate::error::AppError>;
    fn name(&self) -> &'static str;
}
```

### 7.2 火山实现 `volcano.rs`（对接要点）

- 火山引擎/豆包**录音文件识别**：上传音频（或 URL），轮询/回调取结果。
- 鉴权用设置页的 `app_id` + `access_token`（+ `cluster`/资源 ID，按火山控制台）。
- 返回 JSON 含整句文本与 `utterances`（带 `start_time`/`end_time` 毫秒）→ 映射到 `AsrResult.words`。
- 非 wav/采样率不符时，先用打包的 `ffmpeg` sidecar 转 16k 单声道 wav。
- 具体字段名/接口路径以火山官方文档为准（执行方对接时核对），本层只暴露统一 `AsrResult`。

### 7.3 错误策略

- ASR 失败（鉴权/网络/限流/解码）→ `asr_status='failed'`，提交进入"待人工确认"或 `anomaly_type='audio_invalid'`（解码失败）。**不阻塞**导入主流程。
- 看板/异常池可对单条"重试 ASR"。
- 无 `words` 时间戳时，熟练度降级（仅按总时长估算语速），`confidence` 降低并标注。

---

## 8. 评分（纯算法，无 LLM）

> 产出两个独立维度分 + 一个综合参考分，全部确定性、可单测、可解释。系统**不写** `human_result`，通过/未通过由人工点选。

### 8.1 正确率 `accuracy_score`（`domain::accuracy`）

衡量"背对了多少内容"。

1. **归一化**（`normalize`）：全角转半角、删标点/空白、可选删语气词、中文数字统一。
2. **可选拼音容错**：开启后，将答案与 ASR 文本转拼音再比对，容忍 ASR 同音错字（用 Rust `pinyin` crate）。
3. **相似度**：
   - `lcs_ratio = LCS(answer, asr) / len(answer)`
   - `edit_ratio = 1 - Levenshtein(answer, asr) / max(len(answer), len(asr))`
   - `accuracy = round(100 * (0.6*lcs_ratio + 0.4*edit_ratio))`（权重可配）
4. **拆分指标**（写入 `metrics_json`）：漏背率（答案中未匹配字符比例）、多余率、错字率、最长连续匹配段。

### 8.2 熟练度 `fluency_score`（`domain::fluency`）

衡量"背得顺不顺"，依赖 ASR 时间戳与文本特征。

指标（均可配权重，最终 `fluency = 100 - 加权惩罚`）：

- **语速** `chars_per_sec = 有效字数 / duration_ms*1000`；过慢/过快按区间扣分。
- **停顿率** `pause_ratio = 词间静音总时长 / duration_ms`（由相邻 word 的 end→next start 计算）；多停顿扣分。
- **语气词/卡顿** `filler_count`：统计"嗯/啊/那个/这个/呃"等（可配词表）出现次数。
- **重复/自我纠正** `repetition`：相邻重复片段计数。

> 无时间戳时，仅用 `chars_per_sec`（字数/总时长）估算，其余惩罚置 0，`confidence` 标低。

### 8.3 综合参考分 `ref_score`（`domain::score`）

```
ref_score = round(w_acc * accuracy_score + w_flu * fluency_score)
默认 w_acc=0.7, w_flu=0.3（可在设置页调整）
```

`machine_note` 由算法拼装可读说明，例：「正确率 88（漏背 1 句、错字 3），熟练度 72（停顿偏多、语气词 5 次）」。

### 8.4 评分编排（`scoring_service`）

```
1. 取 contents.answer_text + 当前 answer_version
2. accuracy = domain::accuracy(answer, asr_text, cfg)
3. fluency  = domain::fluency(asr_words, duration_ms, cfg)
4. ref      = domain::score(accuracy, fluency, weights)
5. 写 verdicts(accuracy_score, fluency_score, ref_score, confidence, scorer='algorithm',
              answer_version, metrics_json, machine_note)
6. submissions.status='scored'
```

评分权重/语气词表/拼音容错开关存 `app_settings`，启动加载、设置页可改。

---

## 9. 补背规则（`domain::makeup` + `task_service`）

### 9.1 触发

1. 人工判 `fail` → 原 task `closed`，生成补背任务（`is_makeup=1`, `source_task_id`=原 id, `due_date`=次日/当日按配置）。
2. 日切（启动/定时检查）：`open` 且 `due_date < 今天` → `expired`，结转生成补背任务。

### 9.2 去重护栏（防无限增殖）

> 同一 `student_id + content_id` **同时只允许一条未关闭补背任务**（status ∈ open/submitted/reopened）。生成前先查，已存在则不新建。

```rust
fn ensure_makeup(student_id, content_id, source_task_id) {
    if exists_open_makeup(student_id, content_id) { return; }
    create_task(is_makeup=1, source_task_id, due_date=next_due());
}
```

---

## 10. Tauri Commands（接口边界）

> 全部返回 `Result<T, AppError>`；前端 `src/api/*` 一一对应。命名 `snake_case`。

```
# 学生
students_list(filter) -> Vec<Student>
students_upsert(input) -> Student
students_set_enabled(id, enabled) -> ()
students_import_csv(path) -> ImportReport

# 内容
contents_list() -> Vec<Content>
contents_upsert(input) -> Content     // answer_text 变化 → answer_version+1
contents_import_csv(path) -> ImportReport

# 任务
tasks_generate(due_date, scope) -> GenReport
tasks_list_today() -> Vec<TaskView>
tasks_rollover(today) -> RolloverReport

# 导入
import_scan_dir(dir) -> Vec<ParsedPreview>     // 预览，不落库
import_commit(files) -> ImportReport           // 落库 + 触发 ASR/评分
import_retry_asr(submission_id) -> ()

# 提交 / 异常池
submissions_list(filter) -> Vec<SubmissionView>
anomaly_list() -> Vec<SubmissionView>
anomaly_reassign(submission_id, student_id, content_id, task_id?) -> ()
anomaly_void(submission_id) -> ()

# 判定
verdict_get(submission_id) -> Verdict          // 含 accuracy/fluency/ref + metrics
verdict_human_decide(submission_id, result, note) -> ()   // pass|fail|reopen
verdict_rescore(submission_id) -> Verdict                 // 答案版本变化后重判

# 设置
settings_get() -> SettingsView          // 脱敏返回（token 显示 ****）
settings_set_asr(app_id, access_token, cluster, endpoint, language) -> ()
settings_set_scoring(w_acc, w_flu, use_pinyin, filler_words) -> ()
settings_test_asr() -> TestResult       // 对内置样例音频转写验证凭据
```

---

## 11. 关键骨架代码

### 11.1 Rust 错误类型

```rust
// src-tauri/src/error.rs
#[derive(thiserror::Error, Debug)]
pub enum AppError {
    #[error("db error: {0}")] Db(String),
    #[error("not found: {0}")] NotFound(String),
    #[error("parse error: {0}")] Parse(String),
    #[error("asr error: {0}")] Asr(String),
    #[error("config error: {0}")] Config(String),
    #[error("io error: {0}")] Io(String),
}
impl serde::Serialize for AppError {
    fn serialize<S: serde::Serializer>(&self, s: S) -> Result<S::Ok, S::Error> {
        s.serialize_str(&self.to_string())
    }
}
```

### 11.2 文件名解析（纯函数 + 单测）

```rust
// src-tauri/src/domain/filename.rs
pub struct ParsedName {
    pub date: String,        // YYYY-MM-DD
    pub student_no: String,
    pub name: String,
    pub content_no: String,
    pub seq: Option<u32>,
}
pub fn parse(file_stem: &str) -> Result<ParsedName, crate::error::AppError> {
    let parts: Vec<&str> = file_stem.split('_').collect();
    if parts.len() < 4 {
        return Err(crate::error::AppError::Parse(format!("字段不足: {file_stem}")));
    }
    let raw_date = parts[0];
    if raw_date.len() != 8 || !raw_date.chars().all(|c| c.is_ascii_digit()) {
        return Err(crate::error::AppError::Parse(format!("日期非法: {raw_date}")));
    }
    let date = format!("{}-{}-{}", &raw_date[0..4], &raw_date[4..6], &raw_date[6..8]);
    let seq = parts.get(4).and_then(|s| s.parse::<u32>().ok());
    Ok(ParsedName {
        date, student_no: parts[1].into(), name: parts[2].into(),
        content_no: parts[3].into(), seq,
    })
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test] fn ok() {
        let p = parse("20260625_2023001_张三_C012_1").unwrap();
        assert_eq!(p.date, "2026-06-25");
        assert_eq!(p.student_no, "2023001");
        assert_eq!(p.content_no, "C012");
        assert_eq!(p.seq, Some(1));
    }
    #[test] fn bad_date() { assert!(parse("2026_2023001_张三_C012").is_err()); }
    #[test] fn missing_fields() { assert!(parse("20260625_2023001").is_err()); }
}
```

### 11.3 正确率算法（纯函数 + 单测）

```rust
// src-tauri/src/domain/accuracy.rs
pub struct AccuracyResult { pub score: f64, pub lcs_ratio: f64, pub edit_ratio: f64, pub miss_ratio: f64 }

fn lcs_len(a: &[char], b: &[char]) -> usize {
    let (n, m) = (a.len(), b.len());
    let mut dp = vec![0usize; m + 1];
    for i in 1..=n {
        let mut prev = 0;
        for j in 1..=m {
            let tmp = dp[j];
            dp[j] = if a[i-1]==b[j-1] { prev + 1 } else { dp[j].max(dp[j-1]) };
            prev = tmp;
        }
    }
    dp[m]
}
fn levenshtein(a: &[char], b: &[char]) -> usize {
    let (n, m) = (a.len(), b.len());
    let mut dp: Vec<usize> = (0..=m).collect();
    for i in 1..=n {
        let mut prev = dp[0]; dp[0] = i;
        for j in 1..=m {
            let tmp = dp[j];
            let cost = if a[i-1]==b[j-1] {0} else {1};
            dp[j] = (dp[j]+1).min(dp[j-1]+1).min(prev+cost);
            prev = tmp;
        }
    }
    dp[m]
}
/// answer/asr 已归一化（外层调用 normalize）
pub fn score(answer: &str, asr: &str, w_lcs: f64, w_edit: f64) -> AccuracyResult {
    let a: Vec<char> = answer.chars().collect();
    let b: Vec<char> = asr.chars().collect();
    if a.is_empty() { return AccuracyResult{score:0.0,lcs_ratio:0.0,edit_ratio:0.0,miss_ratio:1.0}; }
    let lcs = lcs_len(&a, &b) as f64;
    let lcs_ratio = (lcs / a.len() as f64).min(1.0);
    let edit = levenshtein(&a, &b) as f64;
    let edit_ratio = (1.0 - edit / a.len().max(b.len()) as f64).max(0.0);
    let s = 100.0 * (w_lcs*lcs_ratio + w_edit*edit_ratio);
    AccuracyResult { score: s.round(), lcs_ratio, edit_ratio, miss_ratio: 1.0 - lcs_ratio }
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test] fn perfect() { assert_eq!(score("床前明月光","床前明月光",0.6,0.4).score, 100.0); }
    #[test] fn partial() { assert!(score("床前明月光","床前明月",0.6,0.4).score < 100.0); }
    #[test] fn empty_answer() { assert_eq!(score("","abc",0.6,0.4).score, 0.0); }
}
```

### 11.4 熟练度算法（骨架）

```rust
// src-tauri/src/domain/fluency.rs
use crate::services::asr::AsrWord;
pub struct FluencyCfg { pub fillers: Vec<String>, pub ideal_cps: f64 } // cps=字/秒
pub struct FluencyResult { pub score: f64, pub cps: f64, pub pause_ratio: f64, pub filler_count: u32, pub confidence: f64 }

pub fn score(text: &str, words: &[AsrWord], duration_ms: u64, cfg: &FluencyCfg) -> FluencyResult {
    let dur_s = (duration_ms as f64 / 1000.0).max(0.001);
    let n_chars = text.chars().filter(|c| !c.is_whitespace()).count() as f64;
    let cps = n_chars / dur_s;

    // 停顿率：相邻词间隙累加
    let mut gap = 0u64;
    for w in words.windows(2) { if w[1].start_ms > w[0].end_ms { gap += w[1].start_ms - w[0].end_ms; } }
    let pause_ratio = (gap as f64 / duration_ms.max(1) as f64).min(1.0);

    // 语气词计数
    let filler_count = cfg.fillers.iter()
        .map(|f| text.matches(f.as_str()).count() as u32).sum();

    // 惩罚：语速偏离、停顿、语气词
    let speed_pen = ((cfg.ideal_cps - cps).abs() / cfg.ideal_cps).min(1.0) * 30.0;
    let pause_pen = pause_ratio * 40.0;
    let filler_pen = (filler_count as f64 * 5.0).min(30.0);
    let s = (100.0 - speed_pen - pause_pen - filler_pen).max(0.0);
    let confidence = if words.is_empty() { 0.4 } else { 0.9 };
    FluencyResult { score: s.round(), cps, pause_ratio, filler_count, confidence }
}
```

### 11.5 前端 invoke 封装

```ts
// src/api/client.ts
import { invoke } from "@tauri-apps/api/core";
export async function call<T>(cmd: string, args?: Record<string, unknown>): Promise<T> {
  try { return await invoke<T>(cmd, args); }
  catch (e) {
    console.error(`[${cmd}]`, e);
    throw new Error(typeof e === "string" ? e : "操作失败");
  }
}
```

```ts
// src/api/import.ts
import { call } from "./client";
import type { ParsedPreview, ImportReport } from "../types/models";
export const scanDir = (dir: string) => call<ParsedPreview[]>("import_scan_dir", { dir });
export const commitImport = (files: string[]) => call<ImportReport>("import_commit", { files });
```

---

## 12. 分步实施手册（一步步操作）

> 每步执行完跑验证，绿了再进下一步，**不要跳步**。

### Step 0 · 环境检查
```bash
node -v        # ≥ 20
cargo --version
rustup target add aarch64-apple-darwin x86_64-apple-darwin
```

### Step 1 · 初始化 Tauri v2
```bash
npm create tauri-app@latest recitation-review -- --template react-ts --manager npm
cd recitation-review && npm install
npm run tauri dev   # 确认空窗口能起
```

### Step 2 · Rust 依赖
`src-tauri/Cargo.toml` 增加：`rusqlite`(bundled), `r2d2`, `r2d2_sqlite`, `serde`, `serde_json`, `thiserror`, `anyhow`, `async-trait`, `reqwest`(json,rustls-tls), `sha2`, `tokio`, `chrono`, `chrono-tz`, `pinyin`(可选)。
```bash
cd src-tauri && cargo build && cd ..
```

### Step 3 · 数据库与迁移
- 写 `0001_init.sql`（§4）；`db/mod.rs` 启动建池、执行迁移、`PRAGMA foreign_keys=ON`。
- 验证：启动后应用数据目录生成 `data.db`，含全部表。

### Step 4 · domain 纯算法 + 单测（先测后用）
- `filename` / `hashing` / `normalize` / `accuracy` / `fluency` / `score` / `makeup`。
```bash
cd src-tauri && cargo test && cd ..
```

### Step 5 · repo 仓储层
- 各表 CRUD；`file_ledger` 按 hash upsert；`tasks` 去重生成。

### Step 6 · services
- `import_service`（§6.2）、`task_service`（生成/日切）、`scoring_service`（§8.4）、`asr/volcano`。

### Step 7 · commands + 前端 api 封装
- 注册全部 command（§10），`src/api/*` 一一对应。

### Step 8 · 前端页面
- 顺序：Settings → Manage（学生/内容/CSV）→ tasks_generate → ImportBatch → DashboardToday → AnomalyPool → MakeupPool → Records。

### Step 9 · sidecar：ffmpeg
- `ffmpeg` 放 `src-tauri/binaries/`，命名带 target triple；`tauri.conf.json` 配 `externalBin`；用于转 16k 单声道 wav。

### Step 10 · 集成测试
- 样例：3 学生、2 内容、若干命名录音（含 1 错误命名、1 重复、1 无对应任务）。
- 跑通：生成任务 → 批量导入 → 火山 ASR → 算法评分（正确率+熟练度）→ 看板人工判 → 补背生成。

### Step 11 · 打包
```bash
npm run tauri build
# 产物：src-tauri/target/release/bundle/macos/<App>.app
```

### Step 12 · 安全扫描 + 打 zip（§14.1）
### Step 13 · 飞书发送（§14.2）

---

## 13. 验收标准（Acceptance Standards）

### 13.1 功能验收清单

- [ ] App 在目标 Mac 双击启动；未签名提示可经右键打开/系统设置放行。
- [ ] 导入学生名单（CSV）和内容，生成今日任务。
- [ ] 批量导入符合命名规则的音频，正确匹配 学生/日期/内容/任务。
- [ ] 重复导入同一文件（同 hash）不重复建提交、不重复评分（幂等）。
- [ ] 命名错误 / 学生不匹配 / 任务不存在的文件进入异常池，可改派或作废。
- [ ] 设置页填火山 App ID + Access Token 后，`settings_test_asr` 返回成功。
- [ ] 至少一个测试音频完成火山转写，并生成**正确率分 + 熟练度分 + 综合参考分**（含可读说明）。
- [ ] ASR 失败时主流程不阻塞，提交进入待人工确认/异常，可重试。
- [ ] 看板可听音、看 ASR 文本、看双维度分、人工点 通过/未通过/打回。
- [ ] 未通过任务生成补背；日切后未交任务结转补背。
- [ ] 同一学生同一内容同时只有一条未关闭补背任务（无增殖）。
- [ ] 改答案后 `verdict_rescore` 按新 `answer_version` 重判。
- [ ] 产物为 `.app.zip`，不含源码压缩包、不含 `secrets.json`、无明文 Token。

### 13.2 自动化测试计划

- **单元测试（cargo test）**：文件名解析、hash 去重、文本归一化、正确率(LCS/编辑距离)、熟练度(语速/停顿/语气词)、综合分、补背生成。
- **数据库测试**：任务生成幂等、重复提交处理、异常改派、答案版本重判。
- **集成测试**：样例数据从任务生成到人工确认全链路。
- **ASR 容错**：有效凭据成功；无/错凭据、网络失败 → 可恢复错误状态。
- **打包验收**：`npm run tauri build` 出包，zip 解压后可重新启动。

### 13.3 判定

13.1 全勾 + 13.2 全绿 = v1 通过。

---

## 14. 打包安全扫描与飞书发送

### 14.1 zip + 扫描脚本（示意）
```bash
APP="src-tauri/target/release/bundle/macos/RecitationReview.app"
# 1) 确认无 secrets.json
find "$APP" -name "secrets.json" -print -quit | grep -q . && { echo "❌ 含 secrets.json"; exit 1; }
# 2) 扫描疑似明文凭据（按火山 token 实际前缀/长度规则补充正则）
if grep -rIl --binary-files=without-match -E "access_token|AKID|[A-Za-z0-9]{32,}" "$APP" 2>/dev/null | grep -q . ; then
  echo "⚠️ 检测到疑似凭据，请人工复核包内容后再继续"; fi
# 3) 打 zip
ditto -c -k --sequesterRsrc --keepParent "$APP" RecitationReview.app.zip
echo "✅ 产物：RecitationReview.app.zip"
```

### 14.2 飞书发送
- 目标："发给我"（你的飞书）。
- 用飞书附件脚本/Hermes 解析并发送**真实文件附件**（非链接）。
- 发送后确认接口返回 success，核对 zip 大小与文件名。

---

## 15. 已确认决策 / 假设

1. **ASR = 火山/豆包**（你提供 App ID + Access Token），不用本机 whisper、不用 DeepSeek。
2. **评分 = 纯算法**：正确率（文本相似度 LCS/编辑距离）+ 熟练度（语速/停顿/语气词），**不用任何 LLM**。综合参考分加权，最终人工确认。
3. 技术栈固定 Tauri，不改 Electron / 纯 Web。
4. 不做 Apple 签名/公证；未签名靠右键放行。
5. 火山凭据仅本机配置与测试，不入库源码、不打进包。
6. 日切时区 Asia/Shanghai (UTC+8)。
7. 熟练度依赖火山返回的时间戳；无时间戳时降级为按总时长估算语速，并降低 confidence。

> 审稿通过后，执行方按 §12 逐步进入编码。
