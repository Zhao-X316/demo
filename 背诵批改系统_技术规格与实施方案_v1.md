# 背诵批改系统 · 技术规格与实施方案 v1

> 面向 macOS 本地版（未签名 App）。本文件既是**技术规格说明书**，也是**给执行方（DeepSeek 驱动的编码代理）的一步步操作手册**。
> 阅读对象：1) 审稿人（产品/你）；2) 执行编码的 AI 代理或工程师。
> 交付物：未签名 macOS App 的 `.app.zip`，最终通过飞书附件发送。

---

## 0. 决策说明（必读，关于 DeepSeek 的关键约束）

**约束**：本项目外部付费 API 只允许使用 DeepSeek。

**事实**：DeepSeek API 是**纯文本/推理大模型**（OpenAI 兼容协议，模型 `deepseek-chat` / `deepseek-reasoner`），**不提供语音识别（ASR）能力**。原 v0.1 方案中的"豆包/火山 ASR"在"只用 DeepSeek"前提下不可用。

**解决方案（本方案采用）**：

| 能力 | 方案 | 是否需要云 API | 说明 |
|------|------|----------------|------|
| 语音转文字 ASR | **本机 whisper.cpp**（sidecar 二进制），失败回退 **macOS 原生 Speech** | 否（完全离线、免费） | 中文识别可用，模型文件随 App 数据目录管理 |
| 参考评分 / 语义判分 | **DeepSeek `deepseek-chat`** | 是（用你的 DeepSeek Key） | 在"文本归一化 + LCS/编辑距离"基础上，叠加 DeepSeek 语义打分，输出参考分与理由 |
| 兜底评分（无网/无 Key） | 本机 LCS/编辑距离相似度 | 否 | 保证 DeepSeek 不可用时主流程不阻塞 |

**抽象不变**：`AsrProvider`（ASR 适配层）与 `GraderProvider`（评分适配层）均为可插拔抽象。今天默认 `whisper-local` + `deepseek-grader`；未来若拿到豆包/火山 token，新增一个 provider 即可，无需改主流程。

**凭据原则**：DeepSeek API Key **绝不**写死进源码或打进 App 包。App"设置页"录入，保存到本机应用数据目录（见 §5 安全准则）。

---

## 1. 总体目标与范围

### 1.1 v1 闭环（必须交付）

1. 学生 / 内容 / 任务管理（增删改、启用状态、内容答案版本）。
2. 批量录音导入 + 文件名解析 + hash 去重（幂等导入）。
3. 异常池（解析失败 / 学生不存在 / 任务不存在 / 重复 / 音频异常）+ 人工改派 / 作废。
4. 今日看板（听音、看 ASR 文本、看参考分、人工判定 通过/未通过/打回）。
5. 补背任务（未通过或日切未交 → 生成补背；同一学生同一内容仅一条未关闭补背）。
6. 本机 whisper ASR + DeepSeek 参考评分；人工最终确认。

### 1.2 明确不做（v1 out of scope）

- 不做 Apple 正式签名 / 公证（未签名，靠右键打开放行）。
- 不做多端同步、不做服务器后端、不做账号体系。
- 不做实时流式 ASR（批处理即可）。
- 不自动判定"通过/未通过"——系统只给**参考分**，人工拍板。

---

## 2. 技术准则（Technical Standards）

### 2.1 技术栈（固定，不得替换）

- 桌面框架：**Tauri v2**
- 前端：**React 18 + TypeScript 5 + Vite 5**
- 后端：**Rust（Tauri commands）**
- 数据库：**SQLite**（经 `sqlx` 或 `rusqlite`，本方案用 `rusqlite` + `r2d2` 连接池，零网络依赖、编译简单）
- ASR：**whisper.cpp**（以 Tauri sidecar 形式调用 `whisper-cli` 二进制）
- 评分 LLM：**DeepSeek**（`reqwest` 调用 OpenAI 兼容接口）

### 2.2 版本与工具链基线

- Node.js ≥ 20 LTS，包管理器统一 **npm**（不混用 pnpm/yarn）。
- Rust stable ≥ 1.77，`cargo` 自带。
- Tauri CLI：`@tauri-apps/cli` v2。
- 目标平台：macOS（Apple Silicon 优先 `aarch64-apple-darwin`，同时支持 `x86_64-apple-darwin`）。

### 2.3 代码规范

- TypeScript：`strict: true`；禁用 `any`（必要处用 `unknown` + 收窄）；ESLint + Prettier。
- Rust：`cargo fmt` + `cargo clippy -- -D warnings`；错误统一用 `thiserror` 定义、`anyhow` 仅在最外层。
- 所有 Tauri command 返回 `Result<T, AppError>`，前端统一捕获并提示。
- 时间统一用 ISO-8601 字符串存储（UTC 存储，UI 按本地时区展示）。
- "日切"基准时区：**Asia/Shanghai (UTC+8)**，配置项可改。

### 2.4 分层与依赖方向（单向，禁止反向依赖）

```
React 页面/组件
   ↓ 调用 invoke()
TS API 封装层 (src/api/*)
   ↓ Tauri IPC
Rust commands 层 (src-tauri/src/commands/*)
   ↓
Rust 领域服务层 (services: import / scoring / makeup / asr / grader)
   ↓
Rust 仓储层 (repo: 纯 SQLite 读写)
   ↓
SQLite
```

规则：上层只能依赖下层；`commands` 不直接写 SQL，必须经 `repo`；`services` 不碰 Tauri 类型（便于单测）。

---

## 3. 目录结构（Code Framework · 总览）

```
recitation-review/
├─ package.json
├─ vite.config.ts
├─ tsconfig.json
├─ index.html
├─ src/                              # 前端 React
│  ├─ main.tsx
│  ├─ App.tsx
│  ├─ api/                           # invoke 封装，按领域分文件
│  │  ├─ client.ts                   # 统一 invoke + 错误处理
│  │  ├─ students.ts
│  │  ├─ contents.ts
│  │  ├─ tasks.ts
│  │  ├─ submissions.ts
│  │  ├─ verdicts.ts
│  │  ├─ import.ts
│  │  └─ settings.ts
│  ├─ types/                         # 与 Rust 对齐的 TS 类型（手写或 ts-rs 生成）
│  │  └─ models.ts
│  ├─ pages/
│  │  ├─ DashboardToday.tsx          # 今日看板
│  │  ├─ ImportBatch.tsx             # 批量导入
│  │  ├─ AnomalyPool.tsx             # 异常池
│  │  ├─ MakeupPool.tsx              # 补背任务池
│  │  ├─ Records.tsx                 # 成绩记录
│  │  ├─ Manage.tsx                  # 学生/内容管理
│  │  └─ Settings.tsx                # ASR / DeepSeek 设置
│  ├─ components/
│  │  ├─ AudioPlayer.tsx
│  │  ├─ ScoreBadge.tsx
│  │  └─ DiffView.tsx                # 答案 vs ASR 文本差异高亮
│  └─ styles/
├─ src-tauri/                        # 后端 Rust
│  ├─ Cargo.toml
│  ├─ tauri.conf.json
│  ├─ build.rs
│  ├─ binaries/                      # sidecar：whisper-cli-<target-triple>
│  ├─ migrations/                    # SQL 迁移，按序号命名
│  │  └─ 0001_init.sql
│  └─ src/
│     ├─ main.rs
│     ├─ error.rs                    # AppError (thiserror)
│     ├─ state.rs                    # AppState：连接池 + 配置路径
│     ├─ db/
│     │  ├─ mod.rs                   # 连接池、迁移执行
│     │  └─ repo/                    # 仓储层（纯 SQL）
│     │     ├─ students.rs
│     │     ├─ contents.rs
│     │     ├─ tasks.rs
│     │     ├─ submissions.rs
│     │     ├─ verdicts.rs
│     │     └─ file_ledger.rs
│     ├─ domain/                     # 纯逻辑，可单测，不依赖 Tauri
│     │  ├─ filename.rs              # 文件名解析
│     │  ├─ hashing.rs              # 文件 hash
│     │  ├─ normalize.rs            # 文本归一化
│     │  ├─ similarity.rs           # LCS / 编辑距离
│     │  └─ makeup.rs               # 补背任务生成规则
│     ├─ services/
│     │  ├─ import_service.rs
│     │  ├─ scoring_service.rs      # 编排 normalize+similarity+grader
│     │  ├─ task_service.rs         # 任务生成/日切结转
│     │  ├─ asr/
│     │  │  ├─ mod.rs               # trait AsrProvider
│     │  │  ├─ whisper_local.rs     # 默认
│     │  │  └─ macos_speech.rs      # 回退（可选）
│     │  └─ grader/
│     │     ├─ mod.rs               # trait GraderProvider
│     │     ├─ deepseek.rs          # DeepSeek 评分
│     │     └─ local_lcs.rs         # 兜底评分
│     ├─ config/
│     │  └─ secrets.rs              # 凭据读写（应用数据目录）
│     └─ commands/                  # Tauri command，薄封装
│        ├─ mod.rs
│        ├─ students.rs
│        ├─ contents.rs
│        ├─ tasks.rs
│        ├─ submissions.rs
│        ├─ import.rs
│        ├─ verdicts.rs
│        └─ settings.rs
├─ tests/                            # 集成测试（Rust）
└─ docs/
   └─ 背诵批改系统_技术规格与实施方案_v1.md   # 本文件
```

---

## 4. 数据模型（SQLite Schema）

> 文件：`src-tauri/migrations/0001_init.sql`。所有表带 `created_at`/`updated_at`（ISO-8601 文本）。
> 音频**只存路径 + hash**，不默认复制进 App；导入后可选归档到应用数据目录。

```sql
PRAGMA foreign_keys = ON;

-- 学生
CREATE TABLE students (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    student_no      TEXT NOT NULL UNIQUE,        -- 学号：权威匹配字段
    name            TEXT NOT NULL,               -- 姓名：仅校验
    class_name      TEXT,
    enabled         INTEGER NOT NULL DEFAULT 1,  -- 0/1
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);

-- 背诵内容
CREATE TABLE contents (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    content_no      TEXT NOT NULL UNIQUE,        -- 内容编号：文件名中的匹配字段
    title           TEXT NOT NULL,
    answer_text     TEXT NOT NULL,               -- 答案正文（当前版本）
    answer_version  INTEGER NOT NULL DEFAULT 1,  -- 答案版本，改答案 +1
    enabled         INTEGER NOT NULL DEFAULT 1,
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);

-- 任务（学生×内容×应完成日期）
CREATE TABLE tasks (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id      INTEGER NOT NULL REFERENCES students(id),
    content_id      INTEGER NOT NULL REFERENCES contents(id),
    due_date        TEXT NOT NULL,               -- YYYY-MM-DD（应完成日期，按 Asia/Shanghai）
    status          TEXT NOT NULL DEFAULT 'open',-- open|submitted|passed|failed|reopened|closed|expired
    is_makeup       INTEGER NOT NULL DEFAULT 0,
    source_task_id  INTEGER REFERENCES tasks(id),-- 补背任务指向原任务
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL,
    UNIQUE(student_id, content_id, due_date, is_makeup)
);
CREATE INDEX idx_tasks_due ON tasks(due_date, status);
CREATE INDEX idx_tasks_student ON tasks(student_id);

-- 提交（一条录音 = 一次提交）
CREATE TABLE submissions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id         INTEGER REFERENCES tasks(id),     -- 可空：异常时未关联
    student_id      INTEGER REFERENCES students(id),  -- 解析得到，可空
    content_id      INTEGER REFERENCES contents(id),  -- 解析得到，可空
    file_path       TEXT NOT NULL,                    -- 原始绝对路径
    archived_path   TEXT,                             -- 归档后路径（可空）
    file_hash       TEXT NOT NULL,                    -- sha256，去重键
    submit_date     TEXT,                             -- 文件名解析出的日期
    parsed_meta     TEXT,                             -- JSON：原始解析结果
    asr_text        TEXT,                             -- ASR 文本（可空）
    asr_status      TEXT NOT NULL DEFAULT 'pending',  -- pending|ok|failed
    anomaly_type    TEXT,                             -- NULL 表示正常；见 §6
    status          TEXT NOT NULL DEFAULT 'pending',  -- pending|scored|confirmed|anomaly|voided
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
CREATE UNIQUE INDEX uq_submissions_hash ON submissions(file_hash);
CREATE INDEX idx_submissions_status ON submissions(status, anomaly_type);

-- 判定（参考分 + 人工结论）
CREATE TABLE verdicts (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    submission_id   INTEGER NOT NULL REFERENCES submissions(id),
    ref_score       REAL,                        -- 参考分 0~100
    confidence      REAL,                        -- 0~1
    grader          TEXT,                        -- deepseek|local_lcs
    answer_version  INTEGER NOT NULL,            -- 评分所依据的答案版本（用于重判）
    machine_note    TEXT,                        -- 机器理由（DeepSeek 返回）
    human_result    TEXT,                        -- pass|fail|reopen|NULL(未判)
    human_note      TEXT,
    decided_by      TEXT,                        -- 留作未来多用户
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
CREATE INDEX idx_verdicts_submission ON verdicts(submission_id);

-- 文件账本（幂等保证：hash 出现过即跳过/指向已有提交）
CREATE TABLE file_ledger (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    file_hash       TEXT NOT NULL UNIQUE,
    first_path      TEXT NOT NULL,
    submission_id   INTEGER REFERENCES submissions(id),
    seen_count      INTEGER NOT NULL DEFAULT 1,
    created_at      TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);

-- 设置（非敏感配置；敏感凭据另存，见 §5）
CREATE TABLE app_settings (
    key             TEXT PRIMARY KEY,
    value           TEXT NOT NULL,
    updated_at      TEXT NOT NULL
);
```

### 4.1 状态机

- `tasks.status`：`open → submitted → (passed | failed)`；`failed → reopened`（生成补背新任务，原任务置 `closed`）；日切未交 `open → expired`（结转补背）。
- `submissions.status`：`pending → scored → confirmed`；异常分支 `→ anomaly`；作废 `→ voided`。
- `submissions.asr_status`：`pending → ok | failed`。

---

## 5. 安全准则（凭据管理）

### 5.1 存储位置

- 应用数据目录（Tauri `app_data_dir()`）：`~/Library/Application Support/<bundle-id>/`
  - `data.db`：SQLite。
  - `secrets.json`：敏感凭据（DeepSeek Key 等），**权限 0600**。
  - `models/`：whisper 模型文件。
  - `archive/`：可选音频归档。

### 5.2 secrets.json 结构

```json
{
  "deepseek": {
    "api_key": "",
    "base_url": "https://api.deepseek.com",
    "model": "deepseek-chat"
  },
  "asr": {
    "provider": "whisper-local",
    "whisper_model_path": "",
    "language": "zh"
  }
}
```

### 5.3 红线

1. 源码、`tauri.conf.json`、任何打进 App 包的资源中**不得**出现真实 Key。
2. `secrets.json` 加入 `.gitignore`；仓库只提交 `secrets.example.json`。
3. 日志、错误提示、`parsed_meta` 中**不得**回显 Key（打印时脱敏为 `sk-****`）。
4. 打包脚本在产出 `.app.zip` 前自动扫描包内是否含 `sk-` 前缀字符串，命中即报错中止（见 §9.3）。
5. 发飞书前再次确认 zip 内无 `secrets.json`、无源码压缩包。

---

## 6. 文件导入与异常规则

### 6.1 命名规范

默认：`YYYYMMDD_学号_姓名_内容编号[_序号].扩展名`
例：`20260625_2023001_张三_C012_1.m4a`

- **学号**为权威匹配字段；**姓名**只做一致性校验（不一致 → 标记 `name_mismatch` 警告，不阻塞）。
- 支持扩展名：`.m4a .mp3 .wav .aac .flac`（可配置）。
- 同一学生同一天可多任务，靠**内容编号**区分。

### 6.2 解析流程（`import_service`）

```
对每个文件：
1. 计算 sha256 → 查 file_ledger
   命中 → 记 seen_count+1，跳过建提交（幂等），返回 "duplicate"
2. 解析文件名 → (date, student_no, name, content_no, seq?)
   失败 → submissions.anomaly_type = 'parse_error'，status='anomaly'
3. 按 student_no 查 students
   不存在/停用 → anomaly_type = 'student_not_found'
4. 按 content_no 查 contents
   不存在/停用 → anomaly_type = 'content_not_found'
5. 匹配 task：student_id + content_id + due_date(=解析date)
   不存在 → anomaly_type = 'task_not_found'
6. 姓名校验：name 与 students.name 不一致 → 记 warning（仍继续）
7. 全部通过 → 建 submission（关联 task/student/content），status='pending'
   写 file_ledger，task.status → submitted
8. 触发异步 ASR + 评分流水线
```

### 6.3 异常类型（`anomaly_type`）枚举

`parse_error` | `student_not_found` | `content_not_found` | `task_not_found` | `duplicate` | `audio_invalid` | `name_mismatch`(warning)

### 6.4 异常池操作

- **改派**：手动指定 student/content/task，重走第 7~8 步。
- **作废**：`status='voided'`，从待办移除（保留账本）。
- **新建任务后重试**：补建 task 后对异常提交一键重匹配。

---

## 7. ASR 适配层

### 7.1 trait

```rust
// src-tauri/src/services/asr/mod.rs
#[async_trait::async_trait]
pub trait AsrProvider: Send + Sync {
    /// 返回识别文本；失败返回可恢复错误（不 panic）
    async fn transcribe(&self, audio_path: &std::path::Path, lang: &str)
        -> Result<String, crate::error::AppError>;
    fn name(&self) -> &'static str;
}
```

### 7.2 默认实现 whisper_local

- 通过 Tauri sidecar 调用 `whisper-cli`（whisper.cpp 编译产物）。
- 命令示例：`whisper-cli -m <model.bin> -f <audio> -l zh -otxt -of <out_prefix>`，读取 `<out_prefix>.txt`。
- 音频非 wav 时先用打包的 `ffmpeg` sidecar 转 16k 单声道 wav（或用 whisper.cpp 自带解码）。
- 模型文件不打进包（体积大）；首次运行引导用户在"设置页"选择/下载 `ggml-base`/`ggml-small` 中文可用模型，存 `models/`。

### 7.3 错误策略

- ASR 失败 → `submissions.asr_status='failed'`，`status` 进入"待人工确认"或保留 `anomaly_type='audio_invalid'`（解码失败时）。**不阻塞**导入主流程。
- 可在异常池/看板对单条"重试 ASR"。

---

## 8. 评分适配层（DeepSeek）

### 8.1 trait

```rust
// src-tauri/src/services/grader/mod.rs
pub struct GradeInput<'a> {
    pub answer_text: &'a str,
    pub asr_text: &'a str,
    pub answer_version: i64,
}
pub struct GradeOutput {
    pub ref_score: f64,     // 0~100
    pub confidence: f64,    // 0~1
    pub note: String,       // 理由
    pub grader: String,     // "deepseek" | "local_lcs"
}
#[async_trait::async_trait]
pub trait GraderProvider: Send + Sync {
    async fn grade(&self, input: GradeInput<'_>) -> Result<GradeOutput, crate::error::AppError>;
}
```

### 8.2 评分编排（`scoring_service`）

```
1. normalize(answer_text) / normalize(asr_text)  // 去标点、全半角、空白、可选去语气词
2. local = LCS+编辑距离相似度 → base_score (0~100)
3. 若 DeepSeek 可用(有 key 且网络通)：
     deepseek.grade() → ref_score + note
   否则：
     ref_score = base_score，grader="local_lcs"，note="DeepSeek 不可用，使用本机相似度"
4. 写 verdicts(ref_score, confidence, grader, answer_version, machine_note)
5. submissions.status = 'scored'（人工未判）
```

> 系统**只产出参考分**，不写 `human_result`。通过/未通过由人工在看板点选。

### 8.3 DeepSeek 调用规格

- 协议：OpenAI 兼容。Endpoint：`{base_url}/chat/completions`（`base_url` 默认 `https://api.deepseek.com`）。
- Header：`Authorization: Bearer <api_key>`，`Content-Type: application/json`。
- Model：`deepseek-chat`（默认）。
- 要求模型**只返回 JSON**（`response_format: {"type":"json_object"}`），结构：
  ```json
  {"ref_score": 0-100, "confidence": 0-1, "note": "中文理由，指出漏背/错背/顺序问题"}
  ```
- Prompt 模板（system）：
  > 你是背诵批改助手。给定"标准答案"和"学生口述的语音识别文本（可能有同音错字）"，评估学生背诵的完整度与准确度，输出 0-100 参考分。请容忍 ASR 同音错字（按读音判断是否背对），重点看内容是否完整、顺序是否正确、是否漏句。只输出 JSON。
- 失败（超时/4xx/5xx/限流）→ 回退 `local_lcs`，记录 `note`。请求超时建议 30s，重试 1 次（指数退避）。

### 8.4 文本归一化与相似度（`domain`，纯函数，必须单测）

- normalize：转小写（中文无影响）、全角转半角、删除标点/空白、可选删除常见语气词（"嗯/啊/那个"）。
- similarity：`相似度 = LCS长度 / max(len(a),len(b))` 与 `1 - 编辑距离/maxlen` 取加权；映射到 0~100。

---

## 9. 补背规则（`domain::makeup` + `task_service`）

### 9.1 触发

1. 人工判 `fail` → 原 task `closed`，生成补背任务（`is_makeup=1`, `source_task_id=原id`, `due_date`=次日或当日，按配置）。
2. 日切（定时/启动时检查）：`open` 且 `due_date < 今天` 的任务 → `expired`，结转生成补背任务。

### 9.2 去重护栏（防无限增殖）

> 同一 `student_id + content_id` **同时只允许一条未关闭补背任务**（status ∈ open/submitted/reopened）。
> 生成前先查；已存在则不再新建，只更新（或跳过）。

```rust
// 伪代码
fn ensure_makeup(student_id, content_id, source_task_id) {
    if exists_open_makeup(student_id, content_id) { return; }
    create_task(is_makeup=1, source_task_id, due_date=next_due());
}
```

---

## 10. Tauri Commands（接口边界）

> 全部返回 `Result<T, AppError>`；前端 `src/api/*` 一一对应。命名 `snake_case`。

```
# 学生
students_list(filter) -> Vec<Student>
students_upsert(input) -> Student
students_set_enabled(id, enabled) -> ()
students_import_csv(path) -> ImportReport

# 内容
contents_list() -> Vec<Content>
contents_upsert(input) -> Content     // answer_text 变化时 answer_version+1
contents_import_csv(path) -> ImportReport

# 任务
tasks_generate(due_date, scope) -> GenReport   // 为启用学生×内容生成今日任务
tasks_list_today() -> Vec<TaskView>
tasks_rollover(today) -> RolloverReport        // 日切结转

# 导入
import_scan_dir(dir) -> Vec<ParsedPreview>     // 预览解析结果，不落库
import_commit(files) -> ImportReport           // 落库 + 触发 ASR/评分
import_retry_asr(submission_id) -> ()

# 提交 / 异常池
submissions_list(filter) -> Vec<SubmissionView>
anomaly_list() -> Vec<SubmissionView>
anomaly_reassign(submission_id, student_id, content_id, task_id?) -> ()
anomaly_void(submission_id) -> ()

# 判定
verdict_get(submission_id) -> Verdict
verdict_human_decide(submission_id, result, note) -> ()   // pass|fail|reopen
verdict_rescore(submission_id) -> Verdict                 // 答案版本变化后重判

# 设置
settings_get() -> SettingsView          // 脱敏返回（key 显示 sk-****）
settings_set_deepseek(api_key, base_url, model) -> ()
settings_set_asr(provider, model_path, language) -> ()
settings_test_deepseek() -> TestResult  // 发一条最小请求验证 key
settings_test_asr() -> TestResult       // 对内置样例音频转写
```

---

## 11. 关键骨架代码

### 11.1 Rust 错误类型

```rust
// src-tauri/src/error.rs
#[derive(thiserror::Error, Debug)]
pub enum AppError {
    #[error("db error: {0}")] Db(String),
    #[error("not found: {0}")] NotFound(String),
    #[error("parse error: {0}")] Parse(String),
    #[error("asr error: {0}")] Asr(String),
    #[error("grader error: {0}")] Grader(String),
    #[error("config error: {0}")] Config(String),
    #[error("io error: {0}")] Io(String),
}
impl serde::Serialize for AppError {
    fn serialize<S: serde::Serializer>(&self, s: S) -> Result<S::Ok, S::Error> {
        s.serialize_str(&self.to_string())
    }
}
```

### 11.2 文件名解析（纯函数 + 单测）

```rust
// src-tauri/src/domain/filename.rs
pub struct ParsedName {
    pub date: String,        // YYYY-MM-DD
    pub student_no: String,
    pub name: String,
    pub content_no: String,
    pub seq: Option<u32>,
}
pub fn parse(file_stem: &str) -> Result<ParsedName, crate::error::AppError> {
    let parts: Vec<&str> = file_stem.split('_').collect();
    if parts.len() < 4 {
        return Err(crate::error::AppError::Parse(format!("字段不足: {file_stem}")));
    }
    let raw_date = parts[0];
    if raw_date.len() != 8 || !raw_date.chars().all(|c| c.is_ascii_digit()) {
        return Err(crate::error::AppError::Parse(format!("日期非法: {raw_date}")));
    }
    let date = format!("{}-{}-{}", &raw_date[0..4], &raw_date[4..6], &raw_date[6..8]);
    let seq = parts.get(4).and_then(|s| s.parse::<u32>().ok());
    Ok(ParsedName {
        date,
        student_no: parts[1].to_string(),
        name: parts[2].to_string(),
        content_no: parts[3].to_string(),
        seq,
    })
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn ok() {
        let p = parse("20260625_2023001_张三_C012_1").unwrap();
        assert_eq!(p.date, "2026-06-25");
        assert_eq!(p.student_no, "2023001");
        assert_eq!(p.content_no, "C012");
        assert_eq!(p.seq, Some(1));
    }
    #[test]
    fn bad_date() { assert!(parse("2026_2023001_张三_C012").is_err()); }
    #[test]
    fn missing_fields() { assert!(parse("20260625_2023001").is_err()); }
}
```

### 11.3 DeepSeek 评分客户端（骨架）

```rust
// src-tauri/src/services/grader/deepseek.rs
use serde_json::json;
pub struct DeepSeekGrader { pub api_key: String, pub base_url: String, pub model: String }

#[async_trait::async_trait]
impl super::GraderProvider for DeepSeekGrader {
    async fn grade(&self, input: super::GradeInput<'_>) -> Result<super::GradeOutput, crate::error::AppError> {
        let client = reqwest::Client::new();
        let body = json!({
            "model": self.model,
            "response_format": {"type": "json_object"},
            "messages": [
                {"role":"system","content": SYS_PROMPT},
                {"role":"user","content": format!(
                    "标准答案：\n{}\n\n学生语音识别文本：\n{}\n\n只输出 JSON：{{\"ref_score\":数字,\"confidence\":数字,\"note\":\"...\"}}",
                    input.answer_text, input.asr_text)}
            ],
            "temperature": 0.2
        });
        let resp = client.post(format!("{}/chat/completions", self.base_url))
            .bearer_auth(&self.api_key)
            .json(&body)
            .timeout(std::time::Duration::from_secs(30))
            .send().await
            .map_err(|e| crate::error::AppError::Grader(e.to_string()))?;
        if !resp.status().is_success() {
            return Err(crate::error::AppError::Grader(format!("http {}", resp.status())));
        }
        let v: serde_json::Value = resp.json().await
            .map_err(|e| crate::error::AppError::Grader(e.to_string()))?;
        let content = v["choices"][0]["message"]["content"].as_str().unwrap_or("{}");
        let parsed: serde_json::Value = serde_json::from_str(content)
            .map_err(|e| crate::error::AppError::Grader(e.to_string()))?;
        Ok(super::GradeOutput {
            ref_score: parsed["ref_score"].as_f64().unwrap_or(0.0),
            confidence: parsed["confidence"].as_f64().unwrap_or(0.5),
            note: parsed["note"].as_str().unwrap_or("").to_string(),
            grader: "deepseek".into(),
        })
    }
}
const SYS_PROMPT: &str = "你是背诵批改助手。给定标准答案与学生语音识别文本（可能含同音错字），\
评估背诵完整度与准确度，输出0-100参考分。容忍ASR同音错字按读音判断，重点看内容完整、顺序、漏句。只输出JSON。";
```

### 11.4 前端 invoke 封装

```ts
// src/api/client.ts
import { invoke } from "@tauri-apps/api/core";
export async function call<T>(cmd: string, args?: Record<string, unknown>): Promise<T> {
  try {
    return await invoke<T>(cmd, args);
  } catch (e) {
    // 统一 toast 错误，再抛出
    console.error(`[${cmd}]`, e);
    throw new Error(typeof e === "string" ? e : "操作失败");
  }
}
```

```ts
// src/api/import.ts
import { call } from "./client";
import type { ParsedPreview, ImportReport } from "../types/models";
export const scanDir = (dir: string) => call<ParsedPreview[]>("import_scan_dir", { dir });
export const commitImport = (files: string[]) => call<ImportReport>("import_commit", { files });
```

---

## 12. 分步实施手册（给执行方一步步操作）

> 每一步执行完跑对应验证命令，绿了再进下一步。**不要跳步**。

### Step 0 · 环境检查
```bash
node -v        # ≥ 20
cargo --version
rustup target add aarch64-apple-darwin x86_64-apple-darwin
```

### Step 1 · 初始化 Tauri v2 项目
```bash
npm create tauri-app@latest recitation-review -- --template react-ts --manager npm
cd recitation-review
npm install
npm run tauri dev   # 确认空窗口能起
```

### Step 2 · 加 Rust 依赖
`src-tauri/Cargo.toml` 增加：`rusqlite`(bundled), `r2d2`, `r2d2_sqlite`, `serde`, `serde_json`, `thiserror`, `anyhow`, `async-trait`, `reqwest`(json, rustls-tls), `sha2`, `tokio`, `chrono`, `chrono-tz`。
```bash
cd src-tauri && cargo build && cd ..
```

### Step 3 · 数据库与迁移
- 写 `migrations/0001_init.sql`（§4）。
- `db/mod.rs`：app 启动时建连接池、执行迁移、`PRAGMA foreign_keys=ON`。
- 验证：启动后 `~/Library/Application Support/<bundle-id>/data.db` 生成，含全部表。

### Step 4 · domain 纯逻辑 + 单测（先测后用）
- `filename.rs` / `hashing.rs` / `normalize.rs` / `similarity.rs` / `makeup.rs`。
```bash
cd src-tauri && cargo test && cd ..
```

### Step 5 · repo 仓储层
- 每表 CRUD；`file_ledger` 的 upsert（按 hash）；`tasks` 的去重生成。

### Step 6 · services
- `import_service`（§6.2 流程）、`task_service`（生成/日切）、`scoring_service`（§8.2）。
- `asr/whisper_local`、`grader/deepseek` + `grader/local_lcs`。

### Step 7 · commands + 前端 api 封装
- 注册全部 command（§10），`src/api/*` 一一对应。

### Step 8 · 前端页面
- 顺序：Settings → Manage（学生/内容/CSV 导入）→ tasks_generate → ImportBatch → DashboardToday → AnomalyPool → MakeupPool → Records。

### Step 9 · sidecar：whisper + ffmpeg
- 把 `whisper-cli`、`ffmpeg` 放 `src-tauri/binaries/`，命名带 target triple（如 `whisper-cli-aarch64-apple-darwin`）。
- `tauri.conf.json` 配置 `externalBin`。
- 设置页引导下载/选择 whisper 模型到 `models/`。

### Step 10 · 集成测试
- 准备样例：3 学生、2 内容、若干命名录音（含 1 个错误命名、1 个重复、1 个无对应任务）。
- 跑通：生成任务 → 批量导入 → ASR → DeepSeek 评分 → 看板人工判 → 补背生成。

### Step 11 · 打包
```bash
npm run tauri build
# 产物：src-tauri/target/release/bundle/macos/<App>.app
```

### Step 12 · 安全扫描 + 打 zip（见 §9.3 脚本）
### Step 13 · 飞书发送（见 §14）

---

## 13. 验收标准（Acceptance Standards）

### 13.1 功能验收清单（逐条勾）

- [ ] App 在目标 Mac 双击启动；未签名提示可经右键打开/系统设置放行。
- [ ] 导入学生名单（CSV）和背诵内容，能生成今日任务。
- [ ] 批量导入符合命名规则的音频，正确匹配 学生/日期/内容/任务。
- [ ] 重复导入同一文件（同 hash）不重复建提交、不重复评分（幂等）。
- [ ] 命名错误 / 学生不匹配 / 任务不存在的文件进入异常池，可人工改派或作废。
- [ ] 设置页填 DeepSeek Key 后，`settings_test_deepseek` 返回成功。
- [ ] 至少一个测试音频完成本机 whisper 转写并生成 DeepSeek 参考分（含理由）。
- [ ] DeepSeek 不可用时自动回退本机相似度评分，主流程不阻塞。
- [ ] 看板可听音、看 ASR 文本、看参考分、人工点 通过/未通过/打回。
- [ ] 未通过任务生成补背任务；日切后未交任务结转为补背。
- [ ] 同一学生同一内容同时只有一条未关闭补背任务（无增殖）。
- [ ] 产物为 `.app.zip`，不含源码压缩包、不含 `secrets.json`、无明文 Key。

### 13.2 自动化测试计划

- **单元测试（cargo test）**：文件名解析、hash 去重、文本归一化、相似度、补背生成。
- **数据库测试**：任务生成幂等、重复提交处理、异常改派、答案版本 `verdict_rescore` 重判。
- **集成测试**：样例数据从任务生成到人工确认全链路。
- **ASR/Grader 容错**：有效 key 成功；无 key/错 key/网络失败 → 可恢复错误 + 回退。
- **打包验收**：`npm run tauri build` 出包，zip 解压后可重新启动。

### 13.3 验收判定

所有 13.1 勾选完成 + 13.2 全绿 = v1 通过。

---

## 14. 打包安全扫描与飞书发送

### 14.1 zip + 扫描脚本（示意）
```bash
APP="src-tauri/target/release/bundle/macos/RecitationReview.app"
# 1) 扫描包内是否含明文 key 前缀
if grep -rIl --binary-files=without-match "sk-" "$APP" ; then
  echo "❌ 检测到疑似明文 Key，中止打包"; exit 1
fi
# 2) 确认无 secrets.json
find "$APP" -name "secrets.json" -print -quit | grep -q . && { echo "❌ 含 secrets.json"; exit 1; }
# 3) 打 zip
ditto -c -k --sequesterRsrc --keepParent "$APP" RecitationReview.app.zip
echo "✅ 产物：RecitationReview.app.zip"
```

### 14.2 飞书发送
- 目标："发给我"（你的飞书）。
- 用飞书附件脚本/Hermes 解析并发送**真实文件附件**（非链接）。
- 发送后确认接口返回 success，并核对 zip 大小与文件名。

---

## 15. 待你确认 / 假设

1. **ASR 用本机 whisper（离线免费）** 替代豆包/火山 —— 因 DeepSeek 无 ASR。若你已有豆包/火山 token 且希望用云 ASR，请告知，我新增一个 provider。
2. DeepSeek 模型默认 `deepseek-chat`（评分场景足够、便宜）；如需更强可切 `deepseek-reasoner`。
3. 技术栈固定 Tauri，不改 Electron / 纯 Web。
4. 不做 Apple 签名/公证；未签名靠右键放行。
5. DeepSeek Key 仅本机配置与测试，不入库、不入源码、不打进包。
6. 日切时区 Asia/Shanghai (UTC+8)。

> 审稿通过后，执行方按 §12 逐步进入编码。
