# 教辅系统 · 平台总体架构 v1（大框架）

> 目标：把"背诵批改"升级为一个**可持续扩展的本地教辅平台**。本文件是**平台级架构蓝图**，定义共享内核、模块契约、通用数据模型与扩展方法，便于后续按模块增量补充。
> 配套文档：`背诵批改系统_技术规格与实施方案_v1.md`（= 本平台的**模块一**详细规格）。
> 技术栈与平台基线沿用：Tauri v2 + React/TS + Rust + SQLite，本地、未签名、`.app.zip` 交付。

---

## 0. 设计原则（先读）

1. **模块化单体（Modular Monolith）**：一个 App、一个数据库，内部按模块清晰分层。不拆微服务（本地桌面无必要），但模块边界严格，便于增删。
2. **共享内核 + 业务模块**：通用能力（学生、任务、复习调度、识别、评分、导入、统计、设置）下沉到 `core`；每个业务模块（背诵、题目批改、错题本…）只实现自己的差异部分。
3. **能力抽象、可插拔**：识别（ASR/OCR）、评分（Grader）、导入解析（Parser）、复习调度（Scheduler）都是 trait/接口，模块按需实现或复用。
4. **数据模型通用化**：`students/tasks/memory_cards/submissions/verdicts` 等做成跨模块通用结构（带 `module` 维度），模块专属数据放各自表（命名前缀隔离）。
5. **新增模块零侵入**：加模块 = 新增一个 Rust 子 crate + 一组前端页面 + 注册到模块表，**不改动其他模块**。
6. **一切结论可解释、可复现**：评分/统计为确定性算法（必要时人工确认）；最终判定权在老师。
7. **凭据与隐私**：所有云凭据（火山 ASR、未来 OCR 等）只存本机、不入源码/不打包；学生数据本地存储。

---

## 1. 产品愿景与模块地图

```
                        教辅系统（本地 App）
┌───────────────────────────────────────────────────────────────┐
│  共享内核 Core（学生/班级/学科/知识点 · 任务&复习引擎 · 识别 ·    │
│                评分抽象 · 导入管线 · 统计报表 · 设置&凭据 · 审计） │
├───────────────────────────────────────────────────────────────┤
│  模块一 M1  背诵批改     （已出详规：ASR + 正确率门控 + 梯度复习） │
│  模块二 M2  题目批改分析 （题库 + 作答导入/OCR + 客观题自动批改）  │
│  模块三 M3  错题本/错题分类统计（错因×知识点 + 错题复习 + 报表）   │
│  模块N …    听写 / 作文批改 / 口算 / 学情报告 / 家长导出 …        │
└───────────────────────────────────────────────────────────────┘
```

### 1.1 模块清单与状态

| 模块 | 代号 | 状态 | 一句话 |
|------|------|------|--------|
| 背诵批改 | M1 | 详规已完成 | 录音→ASR→正确率门控(≥95)→熟练度→梯度复习 |
| 题目批改分析 | M2 | 本文给框架 | 题库+学生作答(拍照OCR/录入)→自动+人工批改→正确率/知识点掌握度 |
| 错题本/错题分类统计 | M3 | 本文给框架 | 错题按知识点×错因归集→统计趋势→错题间隔复习 |
| 听写 | M4 | 预留 | 复用 ASR/OCR + 正确率门控 |
| 作文批改 | M5 | 预留 | OCR + 评分量规（rubric），可选后续接 LLM |
| 学情报告/家长导出 | M6 | 预留 | 跨模块聚合 → PDF/Excel |

> 模块间**强复用**：M1 的"梯度复习引擎"= M3 的"错题复习"；M2 产出的错题**自动流入** M3；知识点体系被 M2/M3/报表共用。

---

## 2. 分层架构

```
┌─ 前端 React（Tauri WebView）
│   ├─ shell：导航/路由/模块注册表/全局布局
│   ├─ shared：UI 组件、api client、类型、统计图表
│   └─ modules/*：每个模块自带 pages + api 封装
│        ↓ invoke()
├─ Tauri commands 层（每模块一组，薄封装）
│        ↓
├─ 业务模块服务层 Rust crate（module-recitation / module-exam / module-wrongbook …）
│        ↓ 依赖
├─ 共享内核 core（crate）
│   ├─ domain：纯算法（相似度/调度/统计/解析/归一化）
│   ├─ services：任务引擎、复习引擎、导入管线、识别编排、评分编排
│   ├─ ports：trait 抽象（Recognizer/Grader/Parser/Scheduler/Exporter）
│   └─ repo：通用表仓储 + 迁移框架
│        ↓
└─ SQLite（单库，core 表 + 各模块表）
```

依赖方向（单向）：`模块 → core`；模块之间**不直接依赖**，跨模块协作走 core 的事件/接口（见 §6 错题流入）。

### 2.1 Rust 工作区（Cargo workspace）

```
teaching-suite/
├─ Cargo.toml                      # [workspace] members
├─ crates/
│  ├─ core/                        # 共享内核
│  ├─ module-recitation/           # M1
│  ├─ module-exam/                 # M2
│  └─ module-wrongbook/            # M3
└─ src-tauri/                      # Tauri 应用外壳：聚合各模块的 commands，注册插件
```

> 这样加模块 = 在 `crates/` 加一个 crate + 在 `src-tauri` 注册其 commands。core 与其他模块不动。

### 2.2 前端结构（feature-based）

```
src/
├─ shell/            # 路由、侧边导航、模块注册表 moduleRegistry.ts
├─ shared/           # api/client.ts, components, charts, types/core.ts
└─ modules/
   ├─ recitation/    # pages + api + types
   ├─ exam/
   └─ wrongbook/
```

`moduleRegistry.ts`：声明每个模块的 `key / 名称 / 图标 / 路由 / 启用开关`，shell 据此渲染导航。**加模块只往注册表加一项。**

---

## 3. 共享内核 Core 的能力

| 能力 | 说明 | 模块如何用 |
|------|------|-----------|
| 基础实体 | 学生、班级、学期、学科、**知识点树** | 全模块共用学生/班级；M2/M3/报表用知识点 |
| **任务引擎** | 通用任务：`module + ref_type + ref_id + due_date + status + kind` | M1 背诵任务、M2 作业任务、M3 错题复习任务都是它 |
| **复习引擎（间隔重复）** | `memory_cards` + scheduler（质量→间隔），通用 | M1 背诵复习、M3 错题复习共用 |
| **识别 Recognizer** | `AsrProvider`（音频→文本）/ `OcrProvider`（图片→文本/版面） | M1 用 ASR；M2/M4/M5 用 OCR |
| **评分 Grader** | 抽象评分接口，返回分数+判定+指标 | 各模块实现自己的评分算法 |
| **导入管线 Ingest** | 文件扫描→hash 去重→解析→匹配→**异常池** | 音频(M1)/图片(M2)统一走管线，解析器可替换 |
| **统计/报表内核** | 通用聚合（按学生/班级/知识点/时间/标签）+ 导出 | M3 错题统计、M6 学情报告 |
| 设置&凭据 | secrets.json（云凭据）+ app_settings（参数） | 全模块共用 |
| 审计/账本 | file_ledger 幂等 + 操作审计 | 全模块共用 |

---

## 4. 通用数据模型（Core 表）

> 单库。Core 表跨模块通用，模块专属表加前缀（`rec_` / `exam_` / `wb_`）。仅列关键字段。

```sql
-- 模块注册
CREATE TABLE modules ( key TEXT PRIMARY KEY, name TEXT NOT NULL, enabled INTEGER NOT NULL DEFAULT 1 );

-- 基础实体
CREATE TABLE students ( id INTEGER PK, student_no TEXT UNIQUE, name TEXT, class_id INTEGER, enabled INTEGER, ... );
CREATE TABLE classes  ( id INTEGER PK, name TEXT, term TEXT, ... );
CREATE TABLE subjects ( id INTEGER PK, name TEXT );                    -- 语文/数学/英语…

-- 知识点树（M2/M3/报表共用）
CREATE TABLE knowledge_points (
    id INTEGER PK, subject_id INTEGER, parent_id INTEGER,             -- 自引用成树
    code TEXT, name TEXT, sort INTEGER
);

-- 通用任务（M1/M2/M3 都用；module 区分归属）
CREATE TABLE tasks (
    id INTEGER PK, module TEXT NOT NULL,            -- recitation|exam|wrongbook
    student_id INTEGER, subject_id INTEGER,
    ref_type TEXT, ref_id INTEGER,                  -- 指向模块对象(背诵内容/作业/错题)
    kind TEXT NOT NULL DEFAULT 'normal',            -- normal|makeup|review
    due_date TEXT, status TEXT, source_task_id INTEGER, card_id INTEGER,
    created_at TEXT, updated_at TEXT
);

-- 通用复习卡片（间隔重复，任何"可复习对象"）
CREATE TABLE memory_cards (
    id INTEGER PK, module TEXT NOT NULL,
    student_id INTEGER, ref_type TEXT, ref_id INTEGER,   -- 背诵内容 或 错题
    state TEXT, stage INTEGER, interval_days INTEGER, ease REAL,
    last_quality TEXT, due_date TEXT, reps INTEGER, lapses INTEGER,
    UNIQUE(module, student_id, ref_type, ref_id)
);

-- 通用提交（媒体）：音频/图片统一
CREATE TABLE submissions (
    id INTEGER PK, module TEXT NOT NULL, task_id INTEGER,
    student_id INTEGER, ref_id INTEGER,
    media_type TEXT,                                -- audio|image|text
    file_path TEXT, archived_path TEXT, file_hash TEXT UNIQUE,
    recognized_text TEXT, recognize_meta TEXT,      -- ASR/OCR 结果(含时间戳/版面)
    recognize_status TEXT, anomaly_type TEXT, status TEXT, ...
);

-- 通用判定
CREATE TABLE verdicts (
    id INTEGER PK, submission_id INTEGER, module TEXT,
    primary_score REAL, pass INTEGER,               -- M1=正确率门控；M2=对错
    secondary_score REAL, quality TEXT,             -- M1=熟练度/质量
    metrics_json TEXT, machine_note TEXT,
    human_result TEXT, human_note TEXT, answer_version INTEGER, ...
);

-- 幂等账本 + 设置
CREATE TABLE file_ledger ( file_hash TEXT UNIQUE, submission_id INTEGER, seen_count INTEGER, ... );
CREATE TABLE app_settings ( key TEXT PRIMARY KEY, value TEXT, updated_at TEXT );
```

> 与 M1 现有详规对齐：M1 的 `rec_contents` 即模块专属表；`tasks/memory_cards/submissions/verdicts` 升级为带 `module` 字段的通用表（M1 详规里的同名表是其 `module='recitation'` 的特例，迁移时统一）。

---

## 5. 能力抽象（Ports / Trait）

```rust
// crates/core/src/ports.rs

// 识别：音频/图片 → 文本（+元数据）
#[async_trait] pub trait Recognizer {
    async fn recognize(&self, input: MediaInput<'_>) -> Result<RecognizeResult, CoreError>;
    fn kind(&self) -> RecognizerKind; // Asr | Ocr
}

// 评分：模块各自实现
pub trait Grader {
    fn grade(&self, ctx: GradeContext<'_>) -> Result<GradeResult, CoreError>;
}

// 导入解析：文件名/内容 → 结构化元数据（音频按文件名、试卷可按二维码/版面）
pub trait IngestParser {
    fn parse(&self, file: &IngestFile) -> Result<ParsedMeta, CoreError>;
}

// 复习调度：记忆质量 → 下次间隔（M1/M3 共用默认实现，可覆盖）
pub trait Scheduler {
    fn next(&self, card: &CardState, quality: Quality) -> SchedOut;
}

// 导出：报表 → PDF/Excel
pub trait Exporter { fn export(&self, report: &Report, fmt: ExportFmt) -> Result<Vec<u8>, CoreError>; }
```

- **M1**：`Recognizer=火山ASR`，`Grader=正确率+熟练度`，`Parser=文件名解析`，`Scheduler=默认梯度`。
- **M2**：`Recognizer=OCR`，`Grader=客观题比对/主观题规则`，`Parser=试卷/作答解析`。
- **M3**：复用 `Scheduler` 做错题复习；自身主要是分类统计逻辑。

### 5.1 模块契约（每个业务模块都实现）

```rust
pub trait Module {
    fn key(&self) -> &'static str;                 // "recitation" | "exam" | "wrongbook"
    fn migrations(&self) -> &[Migration];          // 模块专属表迁移
    fn register_commands(&self, builder: &mut tauri::Builder); // 注册自己的 commands
    fn nav(&self) -> NavEntry;                     // 提供前端导航元数据
}
```

> **新增模块的全部接触点**：实现 `Module` + 在 `src-tauri` 注册 + 前端 `moduleRegistry` 加一项。core 与其他模块零改动。

---

## 6. 关键模块框架草案

### 6.1 M2 题目批改分析

**数据（`exam_` 前缀）**
- `exam_questions`：题干、题型(单选/多选/填空/判断/主观)、标准答案、解析、`knowledge_point_id`、难度、分值。
- `exam_papers` / `exam_paper_items`：组卷（作业/试卷）与题目明细。
- `exam_assignments`：班级/学生的作业下发。
- 学生作答：复用通用 `submissions`（`media_type=image` 拍照 或 `text` 录入）→ OCR → `exam_answers`（每题作答 + 对错 + 得分）。

**批改流程**
```
导入学生作答(拍照/扫描) → OCR(Recognizer) → 切题/对齐题号
→ 客观题: 与标准答案自动比对(对/错/得分)
→ 主观题: 规则/关键词/人工(可后续接 LLM 评分，作为可插拔 Grader)
→ 写 verdicts + exam_answers
→ 错题(判错的题) 自动生成 M3 错题记录（见 6.3 流入）
→ 统计: 单生/班级 正确率、知识点掌握度
```

**与 M1 复用**：导入管线、异常池、submissions/verdicts、知识点体系。

### 6.2 M3 错题本 / 错题分类统计

**数据（`wb_` 前缀）**
- `wb_items`：错题记录 = `student_id + question_ref(来源M2题或手动录入) + knowledge_point_id + error_type_id + 截图/文本 + 来源(exam/manual) + 首次错误时间`。
- `error_types`：错因分类表（树/枚举），如 概念不清 / 计算失误 / 审题错误 / 公式记错 / 粗心 / 时间不够。
- `wb_reviews`：错题复习记录（复用 `memory_cards` 做间隔重复，`ref_type='wrongbook'`）。

**统计维度（统计内核）**
- 按 **知识点** 聚合：高频错点 Top-N、掌握度热力。
- 按 **错因** 聚合：错因分布饼图、随时间趋势。
- 按 **学生/班级/学期/学科** 下钻。
- 错题"消灭"看板：错题经复习连续答对 N 次 → 标记掌握。

**错题复习**：直接调 core `Scheduler`，记忆质量来自复习时再次作答的正确情况 → 间隔重复。

### 6.3 跨模块流入（M2 → M3）

- core 提供轻量**领域事件**：M2 判错时发 `WrongAnswerProduced{student, question, knowledge_point, ...}`，M3 订阅并创建 `wb_items`（去重：同生同题已存在未掌握错题则只累加次数）。
- 本地单体实现：core 暴露 `events::publish/subscribe`（同进程回调即可，无需消息队列）。

---

## 7. 统计 / 报表内核

- 通用聚合查询构造器：维度（学生/班级/知识点/错因/时间）× 指标（正确率/错题数/掌握度/复习到期数）。
- 图表前端：shared/charts（柱/饼/趋势/热力）。
- 导出 `Exporter`：PDF（学情报告/家长版）、Excel（明细）。供 M6 学情报告聚合多模块。

---

## 8. 新增模块操作指南（一步步）

> 以后你补新模块时照此走，最小改动。

```
1. crates/ 新建 module-xxx，依赖 core
2. 定义模块专属表迁移（xxx_ 前缀），impl Module::migrations()
3. 复用 core 能力：tasks/memory_cards/submissions/verdicts/Recognizer/Scheduler/统计
   只实现差异：模块的 Grader / IngestParser / 专属表逻辑
4. impl Module + register_commands()，在 src-tauri 注册
5. 前端 src/modules/xxx：pages + api，注册到 shell/moduleRegistry.ts
6. 单测(domain 纯算法) → 集成测试 → 验收清单
```

---

## 9. 技术准则（平台级，沿用 M1 详规并补充）

- 技术栈固定：Tauri v2 + React/TS + Rust + SQLite（见 M1 §2）。
- 单库多模块；迁移按 `core` + 各模块分目录、序号命名，启动顺序执行。
- 通用表带 `module` 字段；模块表带前缀；外键开启。
- 识别/评分/解析/调度/导出皆为 trait，云凭据本机存储不入包（M1 §5 红线全平台适用）。
- 时区 Asia/Shanghai；时间 ISO-8601 存储。
- 测试：domain 纯算法必测；跨模块事件流入需集成测试。

---

## 10. 路线图（建议节奏）

| 阶段 | 内容 | 产出 |
|------|------|------|
| P0 | 搭平台骨架：workspace + core(实体/任务/复习/导入/识别/设置) + shell 前端 | 可运行空壳 + M1 接入点 |
| P1 | **M1 背诵批改**（按现有详规落地）| 首个闭环可用 |
| P2 | 知识点体系 + 统计内核 | 为 M2/M3 铺路 |
| P3 | **M2 题目批改分析**（OCR + 客观题自动批改）| 作业批改可用 |
| P4 | **M3 错题本/统计**（M2 自动流入 + 错题复习 + 报表）| 错题闭环 |
| P5 | M6 学情报告/导出；M4/M5 视需要 | 平台成型 |

> 建议：**先按本架构把 core 抽出来，再落 M1**，避免 M1 写死后难以通用化。若 M1 已先行，则在 P0 做一次"通用化迁移"（把 M1 的 tasks/cards/submissions/verdicts 升级为带 module 字段的 core 表）。

---

## 11. 待你确认 / 假设

1. 平台采用**模块化单体**（单 App 单库），不拆服务——本地桌面场景最合适。
2. **复习引擎全平台通用**：背诵复习与错题复习共用一套间隔重复内核。
3. M2 学生作答以**拍照 OCR** 为主、手动录入为辅；OCR 同样走**云识别**（与 ASR 一致，凭据本机存）。OCR 具体厂商待定（可火山/腾讯/百度，按你账号）。
4. **知识点体系**为 M2/M3/报表的公共基础，需要先建。
5. M2 主观题 v1 用规则/关键词 + 人工；是否后续引入 LLM 评分由你决定（当前默认不引入）。
6. 现有 M1 详规继续有效，作为本平台模块一；通用化迁移在 P0/P2 进行。

> 你可以基于这个大框架，随时往 §1 模块地图里加模块、往 §6 补模块草案。需要我把某个模块（如 M2 或 M3）展开成像 M1 那样的详细规格时，告诉我模块代号即可。
