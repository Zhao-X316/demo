[package]
name = "module-recitation"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "M1 背诵批改：文件名解析 + 火山ASR + 正确率门控 + 熟练度 + 梯度复习"

[dependencies]
suite-core = { path = "../core" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
async-trait.workspace = true
# 复用 core 同版本 rusqlite（bundled 特性经 cargo 特性合并生效）
rusqlite = "0.31"
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }
regex = "1"

[dev-dependencies]
module-knowledge = { path = "../module-knowledge" }
